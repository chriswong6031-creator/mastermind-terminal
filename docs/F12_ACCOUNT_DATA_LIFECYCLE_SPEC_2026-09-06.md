# F12 — Account data export + deletion lifecycle (records-only spec)

**Packet** B-F12-2 · lane F12 · wave B2 · 2026-09-06 · **Kind: records** (no product code)
**File**: `docs/F12_ACCOUNT_DATA_LIFECYCLE_SPEC_2026-09-06.md` (this file), repo `mastermindx-market-intelligence/mastermind-terminal`, branch `claude/mo-b-b2-b-f12-2`.
*(The commission named `research/…`; this repo's default branch has no `research/` directory — `git ls-tree --name-only origin/master` returns `docs` and no `research`. Per the commission's own fallback, this lands in `docs/`.)*
**Live URL after merge: n/a — records only.** Nothing renders; no nav family is touched; no third header exists to create.
**Ledger rows advanced, NOT closed** (acceptance line 4 is records-only; each row's own `acceptance_test` names a live user action this packet does not perform — see §6): MO-PAID-087 stays PARTIAL (spec + mandatory §4 gate delivered; the ledger's own `next_bounded_child` says DEFER pending Supabase-side verification, which this packet respects by gating §2/§3 behind §4 rather than scoping ahead of it), MO-PAID-086 stays PARTIAL (export spec written; no route exists yet), MO-PAID-078 stays PARTIAL/blocked on 087 (unchanged).

---

## 0. What this document is, and the one thing it is not

It is a build contract. It is **not** a verification of production. In this estate the DDL files are *not* the database: `supabase/migrations/README.md:6-20` records that the `supabase_migrations` schema **does not exist** in project `fsldfzlxyavsuwqbceod` (`select nspname from pg_namespace where nspname='supabase_migrations'` → `[]`; `DSC:TERMINAL-HAS-NO-MIGRATION-LEDGER`), that DDL is applied by hand per-file, and `README.md:51-53` states outright: **"never infer application status from file order. Ask the database."** `0009` was applied two days before `0008` (`README.md:47-48`). So every cascade claim below is **DDL-asserted, production-unverified**, and §4 is a hard gate before §2/§3 are built.

---

## 1. Every user-scoped table in the estate (one Supabase project, two repos writing to it)

Repo codes: **T** = `mastermind-terminal` @ `origin/master`; **M** = `macro` @ `origin/main`.
Columns: FK to `auth.users` · ON DELETE · RLS · owner policy · reachable today by an authenticated user's own request.

| # | Table | DDL (file:line) | FK → auth.users | ON DELETE | RLS | Owner policy | Export route today |
|---|---|---|---|---|---|---|---|
| 1 | `profiles` | T `supabase/migrations/0001_init.sql:10` | `id` | **cascade** | `0001_init.sql:97` | `profiles_self` `0001_init.sql:107-108` | none (read via `/api/me` proxy only) |
| 2 | `watchlists` | T `0001_init.sql:31` | `user_id` | **cascade** | `0001_init.sql:98` | generic owner loop `0001_init.sql:117` | `T terminal/app/api/watchlist/route.ts:62` (GET) |
| 3 | `watchlist_symbols` | T `0001_init.sql:42` | via parent `watchlists(id)` | **cascade (parent)** | `0001_init.sql:99` | `wls_via_parent` `0001_init.sql:131-133` | same route as #2 |
| 4 | `chart_layouts` | T `0001_init.sql:53` (+ unique idx `0008_chart_layouts_unique_name.sql:84`) | `user_id` | **cascade** | `0001_init.sql:100` | generic owner loop `0001_init.sql:117` | `T terminal/app/api/layouts/route.ts:57` (GET) |
| 5 | `saved_scripts` | T `0001_init.sql:64` | `user_id` | **cascade** | `0001_init.sql:101` | `scripts_owner` `0001_init.sql:124` **+ `scripts_public_read` `0001_init.sql:126-127`** | `T terminal/app/api/scripts/list/route.ts:30` (GET) |
| 6 | `alerts` | T `0001_init.sql:78` | `user_id` | **cascade** | `0001_init.sql:102` | generic owner loop `0001_init.sql:117` | `T terminal/app/api/alerts/route.ts:62` (GET) |
| 7 | `favorites` | T `0001_init.sql:89` | `user_id` | **cascade** | `0001_init.sql:103` | generic owner loop `0001_init.sql:117` | **none** |
| 8 | `drawings` | T `0002_drawings.sql:11` | `user_id` | **cascade** | `0002_drawings.sql:21` | `drawings_owner` `0002_drawings.sql:25-26` | `T terminal/app/api/drawings/route.ts:20` (GET, per-symbol) |
| 9 | `brain_threads` | T `0005_brain_threads.sql:16` | `user_id` | **cascade** | `0005_brain_threads.sql:27` | select-own `0005_brain_threads.sql:31-34` (no insert/update/delete policy — service-role only, `0005:10-11`) | `T terminal/app/api/brain/[...path]/route.ts:200` (GET, proxy) |
| 10 | `brain_messages` | T `0005_brain_threads.sql:39` | via parent `brain_threads(id)` | **cascade (parent)** | `0005_brain_threads.sql:50` | select-own via parent `0005_brain_threads.sql:53-61` | same proxy as #9 |
| 11 | `portfolio_positions` | T `0007_portfolio_positions.sql:86`; M `templates/uwp_supabase.sql:36-55` | `user_id` (asserted) | **cascade — UNVERIFIED, weakest cell in this table**: the file's own header says the live table predates all committed DDL and was created by hand (`0007_portfolio_positions.sql:10-11` "Against live production the table, RLS and policy statements are NO-OPS"; `:60-64` "NOT ANONYMOUSLY INTROSPECTABLE ... the FK to `auth.users`" is explicitly flagged unconfirmed; corroborated `templates/uwp_supabase.sql:12-14`) | T `0007:104` / M `uwp_supabase.sql:36` (same unverified status) | 4 policies T `0007:111-127` / M `uwp_supabase.sql:40-55` (unverified) | `T terminal/app/api/portfolio/route.ts:60` (GET) |
| 12 | `search_events` | T `0003_search_events.sql:43` | `user_id` | **set null** | `0003:54` | **deny-all, no policies on purpose** `0003:53` | **none** (service-role only) |
| 13 | `analytics_events` | T `0004_analytics.sql:32`; M `scripts/deploy/0004_analytics.sql:17` | `user_id` | **set null** | T `0004:47` / M `0004:29` | **deny-all** T `0004:46` | **none** |
| 14 | `ip_geo` | T `0004_analytics.sql:50`; M `scripts/deploy/0004_analytics.sql:31` | none (`ip` PK) | n/a | T `0004:71` / M `0004:39` | **deny-all** `0004:70` | **none** — not user-scoped, but joins to #13 by `ip` |
| 15 | `user_entitlements` | M `scripts/deploy/0005_user_entitlements.sql:10` | `user_id` (PK) | **cascade** | `0005:26` | select-own `0005:33` | M `app/main.py:1044` `GET /api/account` (derived fields only) |
| 16 | `stripe_events` | M `scripts/deploy/0005_user_entitlements.sql:40` | none | n/a | `0005:45` | deny-all (no policy statement over RLS — comment `0005:37`, RLS enabled `0005:45`) | **none** |
| 17 | `support_tickets` | M `scripts/deploy/0007_support_email.sql:24,32` | `user_id` | **set null** | `0007:45` | deny-all (`0007:150`) | **none** |
| 18 | `support_ticket_messages` | M `scripts/deploy/0007_support_email.sql:53,55` | via `support_tickets(id)` | **cascade (parent)** — parent is NOT deleted, so effectively **retained** | `0007:64` | deny-all (`0007:151`) | **none** |
| 19 | `email_log` | M `scripts/deploy/0007_support_email.sql:74,81` | **`user_id uuid` with NO `references` clause** | **none — row survives, carrying `to_email` (`0007:80`)** | `0007:92` | deny-all (`0007:152`) | **none** |
| 20 | `email_prefs` | M `scripts/deploy/0007_support_email.sql:99-100` | `user_id` (PK) | **cascade** | `0007:106` | deny-all (`0007:153`) | M `app/main.py:1072-1075` returns `prefs` from auth metadata, not this table |
| 21 | `email_suppression` | M `scripts/deploy/0007_support_email.sql:113-114` | none — **keyed on the address** (`0007:109-111`) | n/a | `0007:119` | deny-all (`0007:154`) | **none** |
| 22 | `email_campaigns` | M `scripts/deploy/0007_support_email.sql:126` | none | n/a | `0007:143` | deny-all (`0007:155`) | **none** — operator artifact |
| 23 | `trade_episodes` | M `scripts/deploy/0008_trade_memory.sql:13` | `user_id` | **cascade** | `0008:59` | 4 owner policies `0008:62-71` | **none** |
| 24 | `trade_memory_patterns` | M `scripts/deploy/0008_trade_memory.sql:77` | `user_id` | **cascade** | `0008:86` | select-own `0008:88` | **none** |
| 25 | `auth.users.user_metadata` | written M `lib/user_prefs.py:160` via `_admin_url` `lib/user_prefs.py:131-135` | is the user | n/a | GoTrue | service-role key `lib/user_prefs.py:117-123` | read-back M `app/main.py:1072-1075` |
| 26 | research-vault download quota ledger | M `engine/research_vault/download_quota.py:55` (`_quota_dir`), `:61` (`_safe_uid`), `:91` (`_ledger_file`) | **filesystem JSON, not Supabase** | **no cascade — survives account deletion** | n/a | n/a | **none** |
| 27 | Stripe customer + subscriptions | M `app/billing.py:643` (`read_entitlement`), `:550` (PostgREST), `:1470` (`_cancel_subscriptions`) | external processor | **outside our database** | n/a | n/a | Stripe portal `T terminal/app/api/billing/portal/route.ts` |

*(**"Export route today" = `none`** cells were verified by grepping both repos' route/handler surfaces — `terminal/app/api/**/route.ts` and macro `app/main.py`, `app/billing.py`, `app/mailer.py`, `app/support.py`, `app/marketing_emails.py`, `app/email_segments.py`, `app/unsubscribe.py` — for a GET endpoint that hands an authenticated user their own rows from that table. Several of those tables have service-role writers/ingesters in those same files — e.g. `app/support.py` for #17/18, `app/mailer.py` for #19 — but none has a user-facing read/export path, which is what the column asserts.)*

**Read of that table (corrected — the enumeration is the source of truth; this prose must match it):**
1. **Fifteen tables are cleared when `auth.users` is deleted** — #1,2,4,5,6,7,8,9,11,15,20,23,24 (thirteen rows) cascade *directly* off `auth.users`, plus #3 (`watchlist_symbols`) and #10 (`brain_messages`) are cleared *transitively* because their parent (`watchlists`, `brain_threads`) itself cascades off `auth.users` — together #1-11,15,20,23,24 (**15/24 ≈ 62%** of the *tables* in this estate — the denominator is rows 1-24 only; #25 is a column on the `auth.users` record, not a separate table, #26 is a filesystem ledger, and #27 is an external processor, so none of the three belongs in a table count, round 3 fix for MINOR 5) *if* production FKs match these files, which §4 (Q1 + Q1b below) must prove. #18 (`support_ticket_messages`) is NOT in this group: its parent `support_tickets` uses `set null`, not cascade-delete, so nothing above deletes it, and §3.2 stage 8 takes no action on it either — round-5's fix for MAJOR-4 leaves it completely untouched, `body` intact, precisely because its parent row is never deleted (**round-6 correction of MAJOR-2**, which found this sentence, and §4 Q1b's gate rule, still describing #18 as "handled" by an explicit stage-8 action that no longer exists: #18 needs no explicit handling anywhere in this document). (#25 `auth.users.user_metadata` is not a separate row to prove via FK at all — it is a column on the `auth.users` record itself, so it disappears the instant stage 5 deletes that record; no Q1/Q1b proof applies to it, and it is not one of the "fifteen".)
2. **Two tables null `user_id` but keep a re-linkable device/browser identifier on the row itself** — `search_events` (#12) keeps `anon_id` (the mm_aid cookie, `0003_search_events.sql:44`); `analytics_events` (#13) keeps `visitor_id`, `fp` and `session_id` (`0004_analytics.sql:29-31`). §3.2 stage 8 does not purge either table by matching these identifier *values* — a shared cookie/fingerprint would make that a cross-subject delete — it purges by the **row `id`** each table's own bigint PK captured at stage 4 while `user_id` still linked it to this subject (§3.2 stage 4/8, §3.5 test 5b), so the identifier columns never need naming as a deletion key at all; whichever value they held goes with the row. `ip_geo` (#14) is different in kind: it is never user-scoped to begin with (keyed on the address itself) and never cascades, so it is retained on purpose rather than purged — see item 3. `stripe_events` (#16) has no FK to `auth.users`, is not queryable by `user_id`, and — per its complete DDL (`0005_user_entitlements.sql:40-44`: `id text primary key, type text, received_at timestamptz`, no other column) — stores **no payload column at all**, so there is no Stripe customer or payment identifier in it to disclose or purge for any specific subject (**round-5 correction of MAJOR-2**, which had assumed a payload column this DDL does not define); it is out of scope for both specs — see item 5. `support_tickets` (#17) has `user_id` explicitly set `null` by stage 8 itself, not left to the FK alone (**round-6 fix for MAJOR-1**: the FK's own `on delete set null` (`0007_support_email.sql:29-32`) most likely already does this the instant stage 5 runs, but its firing in production is unconfirmed by §4's gate, so stage 8 issues its own `update` as a guarantee that does not depend on that confirmation — see §4 Q11), and additionally redacts its `email` column (`0007_support_email.sql:28`) to a fixed placeholder — the row itself is never deleted (**round-5 fix for MAJOR-4**, honouring rather than overriding the DDL's own `on delete set null` intent); `support_ticket_messages` (#18) is left completely untouched — `body` intact — because its parent row still exists. `email_log` (#19) keeps `to_email` with no FK at all, but stage 8's purge deletes every row for that address **except** the one identified by the stage-7 confirmation send's own `idem_key`/`template` (**round-6 fix for MAJOR-4**, replacing a pure `created_at` boundary that also retained other post-request mail) — that one row is retained, not purged — see item 3.
3. **Four things are deliberately RETAINED and disclosed by name, matching §3.2/§3.4 exactly:** `email_suppression` (#21, address-keyed, covers people who never registered — deleting it would silently re-subscribe someone who unsubscribed); `ip_geo` (#14, retained because it is a multi-tenant, address-keyed cache, not a per-subject row); the single `email_log` (#19) row stage 7 itself writes as the deletion confirmation, identified by its own `idem_key`/`template` rather than by timestamp (**round-6 fix for MAJOR-4**, replacing round-5's time-boundary approach, which also retained any *other* mail sent to that address at or after `requested_at`), **retained for 30 days as the record that this deletion ran** and purged afterward by a new nightly job (**round-6 fix for MAJOR-3** — see §3.2 stage 8 and §3.5 test 13); and the user's `support_tickets`/`support_ticket_messages` rows (#17/18), kept in **anonymised** form — `user_id` nulled by the FK, `email` redacted by stage 8, `support_ticket_messages.body` untouched — rather than deleted (**round-5 fix for MAJOR-4**, replacing the explicit delete of both tables carried through rounds 1-4, which contradicted this very item's own claim that #18's parent "is never deleted"). `stripe_events` (#16) is **not** in this list: **round-5 correction (MAJOR-2)** found it carries no per-subject column at all (item 2 above), so there is nothing about *this* subject to retain or disclose from that table specifically — distinct from Stripe's own external records (#27, §2.3 item 4). Aggregate campaign counters (#22) are deliberately **not** in this disclosed set either: `email_campaigns` has no FK to any user (item 5), so it holds nothing about *this* subject to disclose.
4. **Two stores live outside Postgres**: the filesystem quota ledger (#26) and Stripe (#27, the processor's own records — an external concern, distinct from our own `stripe_events` table, which item 5 below covers). Neither is reached by any cascade.
5. **Two tables hold no user-linkable data at all**: `email_campaigns` (#22) has no FK to any user and is an operator artifact (campaign definitions, not per-user rows); `stripe_events` (#16) has no FK to any user either and, per its DDL (`0005_user_entitlements.sql:40-44`), stores only a Stripe event id, a type string and a receive timestamp — no payload, no customer id (**round-5 correction of MAJOR-2**, which had wrongly treated it as carrying disclosable payment identifiers). Both are out of scope for the export and deletion specs below, and neither belongs in the per-subject retention disclosure in item 3.

---

## 2. Export spec (advances MO-PAID-086, does not close it)

### 2.1 Where it lives
**Owner: the macro repo (FastAPI), new route `GET /api/account/export`.** The account panel is macro-hosted and loaded on every macro page (`M templates/theme.js:469-480`, `M templates/seo_base.html.j2:505`, `M scripts/build_site.py:5775`); identity/entitlement already lives at `M app/main.py:1044`; the Terminal is explicitly a thin proxy that never holds a secret key (`T terminal/app/api/billing/gateway.ts:5-6,20-21`). Building the export in the Terminal would need a second privileged path — **forbidden by F12 do_not_redo (no second auth/tenant plane, no secret store)**. The Terminal's only possible change is a link to the macro account panel; this packet builds none.

### 2.2 Contract
`GET /api/account/export`, `Authorization: Bearer <supabase access token>` → 200 JSON attachment `mastermind-export-<YYYY-MM-DD>.json`; 401 no/invalid token; 429 one export per 15 min per user (in-memory sliding-window throttle, same pattern as the existing brain burst-throttle, `app/main.py:1223-1250` — a fresh per-endpoint dict, not the brain's own, since a rate-limit collision across unrelated endpoints would be its own bug); 503 `{error, partial:false}` **only when zero collections could be read at all** (e.g. a total backend outage) — **round 3 fix for MINOR 2, which found this line contradicting §2.2/§2.3's own `unavailable[]` contract:** a bundle missing one or more collections is never a 503, it is always 200 with those collections listed in `unavailable[]` (§2.5 test 4); `partial:false` on the 503 path describes "no bundle was produced", not "no collection is missing".

Body v1: `schema:"mm.account_export.v1"`, `generated_at`, `account{user_id,email,email_confirmed,created_at,display_name,prefs}` — sourced directly from the same `Depends(require_user)` record (`app/main.py:979` `return dict(ident.record)`, the full GoTrue `/auth/v1/user` response, `app/paywall.py:245-285`) every authed route already receives, **never from `/api/account`'s own return dict** (**round-6 fix for MAJOR-5**, correcting a false citation to `app/main.py:1056-1075`: reading those exact lines, that handler returns `authenticated, name, email, email_confirmed, tier, plan_label, status, features, current_period_end, interval, prefs, plans_url` — no `user_id`, no `created_at`, and its display field is `name`, not `display_name`): `user_id` from `user.get("id")` (same pattern as `app/main.py:1048`); `email_confirmed` from `user.get("email_confirmed_at") or user.get("confirmed_at")` (`app/main.py:1060`); `display_name` from `_user_display_name(user)` (`app/main.py:988-1001`); `prefs` from `user.get("user_metadata")` (`lib/user_prefs.py:94`, same shape as `app/main.py:1072-1075`). `created_at` cannot be confirmed from either repo's own files — it is asserted only from Supabase Auth's documented GoTrue user-object shape (an external API contract), so it is flagged **needs production readback**: call `GET {SUPABASE_URL}/auth/v1/user` with a real token and confirm the response carries `created_at` before the build packet relies on it; drop the field rather than fabricate it if absent. `plan{tier,status,interval,current_period_end}` (`app/billing.py:643`), `collections{profiles, watchlists, watchlist_symbols, chart_layouts, saved_scripts, alerts, favorites, drawings, portfolio_positions, brain_threads, brain_messages, trade_episodes, trade_memory_patterns}` (**`profiles` added round-5, MAJOR-3**: its only user-writable column is `display_name` — `T supabase/migrations/0001_init.sql:9-13` defines the table, `0003_search_events.sql:26-27` narrows the client `UPDATE` grant to `display_name` alone; `is_pro` (a UI hint per `terminal/AGENTS.md`, `0006_lock_is_pro.sql:13-14`) and `is_admin` (added `0003_search_events.sql:17`, a service-role-only admin gate with no client-facing meaning at all — grepped both repos for `is_admin`, no reference outside the migrations, **round-6 fix for MINOR-2**, which found this claim carrying no file:line at all) are both locked to service-role-only by that same column-scoped grant (`0003_search_events.sql:26-27`) and neither is exported — so `collections.profiles` names the same field already surfaced at `account.display_name` explicitly, rather than leaving it implicit inside `account`), `counts{<collection>:int}` (present for every collection, 0 included), `not_included[{what,why,how_to_ask}]`, `unavailable[{what,why}]`.

Rows are stored rows, unchanged — no scoring, no derived judgement, no LLM-written summary (A7). `unavailable` is the null-disclosure channel: a failed collection is named there, omitted from `counts`, and the bundle still ships — never a silent drop.

### 2.3 Excluded in v1 (`not_included[{what,why,how_to_ask}]`, EN+ZH, all seven entries below — round 3 adds items 6-7 for MAJOR 5, which found `email_prefs`/#20 and `email_suppression`/#21 disclosed nowhere in the export)
1. **what** (EN) "Site usage records" / (ZH) "网站使用记录" — covers `search_events`, `analytics_events`, `ip_geo`.
   **why** (EN) "These are tracked by a browser/device identifier, not stored as part of your account profile, so they're handled separately rather than bundled into your account file." / (ZH) "这些记录通过浏览器/设备标识追踪，而非作为账户资料的一部分存储，因此会单独处理，不会归入账户数据文件。"
   **how_to_ask** (EN) "Contact support and name this category; we'll explain what's retained and why." / (ZH) "请联系客服并说明此类别；我们会解释保留了哪些内容及原因。"
2. **what** (EN) "Support messages" / (ZH) "客服工单消息" — covers `support_tickets`, `support_ticket_messages`.
   **why** (EN) "Support conversations are kept in anonymised form for our records." / (ZH) "客服对话会以匿名形式保留，作为我们的记录。"
   **how_to_ask** (EN) "Contact support directly — they can pull your ticket history for you." / (ZH) "请直接联系客服，他们可以为你调取工单历史记录。"
3. **what** (EN) "Email delivery records" / (ZH) "邮件发送记录" — covers `email_log`.
   **why** (EN) "This is a record that we sent you an email, not part of your account content." / (ZH) "这只是我们曾向你发送过邮件的记录，不属于账户内容。"
   **how_to_ask** (EN) "Contact support if you need proof a specific email was sent." / (ZH) "如需证明某封邮件已发送，请联系客服。"
4. **what** (EN) "Payment records" / (ZH) "付款记录" — covers Stripe's own records (#27, outside our database) (**round-5 correction of MAJOR-2**, reversing round 3's addition of our own `stripe_events` table here: that table's DDL — `0005_user_entitlements.sql:40-44` — stores only a Stripe event id, a type string and a timestamp, no payload and no per-subject identifier, so there is nothing about *this* user in it to include or exclude from an export).
   **why** (EN) "Your payment processor keeps its own record of your payments, separate from your account file." / (ZH) "支付服务商会单独保存你的付款记录，与账户数据文件分开。"
   **how_to_ask** (EN) "Find your receipts in the billing portal, or ask support for a copy." / (ZH) "可在账单门户中查看收据，或联系客服索取副本。"
5. **what** (EN) "Download allowance counters" / (ZH) "下载额度计数" — covers the research-vault quota ledger.
   **why** (EN) "This is just a monthly usage count, not account content." / (ZH) "这只是每月使用次数统计，不属于账户内容。"
   **how_to_ask** (EN) "Contact support if you want to know your current count." / (ZH) "如需了解当前计数，请联系客服。"
6. **what** (EN) "Marketing preference record" / (ZH) "营销偏好记录" — covers `email_prefs` (#20).
   **why** (EN) "Your current marketing preference already shows in your account settings; this is a separate internal record used to enforce it, not account content." / (ZH) "你当前的营销偏好已在账户设置中显示；此为用于执行该设置的内部记录，不属于账户内容。"
   **how_to_ask** (EN) "Contact support if you want a copy of this record." / (ZH) "如需此记录副本，请联系客服。"
7. **what** (EN) "Do-not-email record" / (ZH) "退订记录" — covers `email_suppression` (#21).
   **why** (EN) "This is a record that an email address asked not to receive marketing mail, kept for compliance rather than as account content." / (ZH) "这是某邮箱要求不再接收营销邮件的记录，出于合规目的保留，不属于账户内容。"
   **how_to_ask** (EN) "Contact support if you want to confirm your unsubscribe status." / (ZH) "如需确认退订状态，请联系客服。"

CSV/zip is explicitly deferred; copy says "JSON only for now". MO-PAID-086's "CSV/JSON snapshot" is therefore **partially** closed: JSON ships, CSV is a named null.

### 2.4 Plain-language copy (account panel, EN / ZH)
Button: "Download my data" / "下载我的数据". Subtext: "A single file with your watchlists, layouts, drawings, alerts, positions, saved scripts and chat threads." / "一个文件，包含你的自选、布局、画线、提醒、持仓、脚本和对话记录。" Running: "Preparing your file…" / "正在准备文件…" Failure: "We could not build your file. Nothing was changed. Try again in a few minutes." / "暂时无法生成文件。你的数据没有任何改动。请几分钟后再试。" Banned: "export job", "payload", "schema", "RLS", any table name.

### 2.5 Export — not done unless (macro `tests/test_account_export.py`)
1. Owner isolation (round 3 fix for MINOR 1, which found this vacuous for the two parent-keyed collections; round-5 adds `profiles`): seeded user B present in every collection; for the eleven collections with a direct user-identifying column — the ten with `user_id`, plus `profiles` whose own `id` IS the user's auth id (`0001_init.sql:10`) — user A's bundle has zero rows/values belonging to B; for the two parent-keyed collections (`watchlist_symbols` via `watchlists`, `brain_messages` via `brain_threads`, neither of which carries `user_id`), zero rows whose parent does not belong to A.
2. No token, no bundle: absent/expired/other-project token → 401, empty body, no Supabase call.
3. Counts match rows per collection; zero-row collections show `0`, not omitted.
4. A failed collection is disclosed, not dropped: injected read failure → 200, that collection in `unavailable`, absent from `counts`, others intact.
5. `not_included` complete: all seven entries present (round 3 adds items 6-7, MAJOR 5), non-empty `what`/`why`/`how_to_ask` in both languages.
6. No fabricated field: every key in `collections.*` maps to a §1-asserted column; a golden fixture pins the key set.
7. Rate limit holds: second call inside 15 min → 429, no Supabase round trip.

---

## 3. Deletion spec (advances MO-PAID-087; MO-PAID-078 stays blocked pending 087)

### 3.1 Contract
`POST /api/account/delete`, `Authorization: Bearer <token>`, body `{confirm_email}` → 200 `{ok:true, receipt}`; 400 mismatch; 401 no/invalid token, including a token whose user id no longer exists (round 2 fix — see §3.5 test 9); 409 already in progress (**round 3 fix for MAJOR 4, which found no mechanism behind this code — see §3.2 stage 0 and §3.5 test 12**, backed by a persisted in-flight marker, not a best-effort in-memory guess); 503 `{ok:false, stage, identity_deleted, receipt(partial)}` — `identity_deleted` is `true` only once stage 5 has **completed successfully** (**round 3 fix for MAJOR 2, which found "has run" mapping a stage-5 call that itself errors, times out, or returns non-2xx to the "your account has been deleted" copy — the identity was never actually deleted in that case; see §3.5 test 11**), so the client can pick the correct partial-failure copy in §3.4.

### 3.2 Order of operations (load-bearing; round 2 repair below fixes an unreachable-rows defect the round-1 reorder introduced)
0. Verify token→user (`app/main.py:952 require_user`, the same secretless Bearer verification every authed route uses); require a typed `confirm_email` match — this is a new mechanism, no existing typed-confirmation precedent exists anywhere in either repo (grepped both repos for `confirm`; nothing but unrelated prose/comments), so the build packet adds it fresh rather than reusing something that doesn't exist. **Round 3 addition (MAJOR 4 — the 409 mechanism):** once token and email match, compute `subject_digest = sha256(user_id+email)` (the same digest §3.3 stores) and insert an `account_deletion_receipts` row with `status='in_progress'` under a partial unique index on `(subject_digest) where status='in_progress'` (§3.3); a conflicting insert means a run for this account is already in flight, and the request returns 409 immediately, before stage 1 touches anything. This is a persisted database row, not an in-memory guess — unlike the existing brain burst-throttle (`app/main.py:1223-1250`), an in-memory single-process pattern not reused here because a deletion run can legitimately outlive one request/process. **Round-6 addition (MINOR-1):** this same insert captures `requested_at = now()` (the row's own `created_at` — no separate capture step is needed, but this document names it explicitly since §3.2 stage 8 and §3.3's receipt both depend on it and stage 0 is the only place it is set; previously no stage claimed to set it at all).
1. Offer (never force) the §2 export first; UI requires a checkbox acknowledgement.
2. Resolve `customer_id` from `user_id` first — **round-6 fix for MINOR-3**, which found no stage did this lookup despite §1 row 27 citing it: use `app/billing.py:598 _existing_customer(user_id)` (verified against the live Stripe mode; internally reads `user_entitlements.stripe_customer_id` via `app/billing.py:560 _stored_customer`, the column defined at `scripts/deploy/0005_user_entitlements.sql:15`, keyed on `user_id` primary key `:10`). If `customer_id` is `None` (the user never had a paid subscription), this stage is a no-op: `billing_cancelled:true` immediately, no cancel/readback call made. Otherwise, cancel live Stripe subscriptions, then verify by **readback** — reuse `app/billing.py:1470 _cancel_subscriptions`, but that function's own docstring says it "Never raises (a cancel failure must not wedge the webhook)" and its body (`app/billing.py:1470-1482`) wraps every Stripe call, including the list call itself, in a try/except that only logs; **the call returning is therefore not evidence of anything, and the spec may not claim a 503 straight from it** (**round-5 fix for MAJOR-1**). Instead, immediately after the call, list the customer's subscriptions again with the same client (`stripe.Subscription.list(customer=customer_id, status="all", limit=20)`, the identical call `_cancel_subscriptions` makes at `app/billing.py:1475`) and require every one has settled at `status` in (`canceled`, `incomplete_expired`); this must precede identity deletion or a charge could land on a vanished account. A subscription still `active`/`trialing`/`past_due` after the readback — or a readback call that itself errors or times out — **is the failable condition**: 503 `stage="cancel_subscriptions"`, `identity_deleted:false`, `billing_cancelled:false` (nothing is asserted as done unless the readback confirms it). Only a clean readback sets receipt field `billing_cancelled:true` (round 2 repair for MAJOR 2 — this flag, not `identity_deleted`, is what lets §3.4 disclose an irreversible side effect that happened before the point of no return). *Known edge case, out of scope for this stage's own pass/fail: if some but not all subscriptions settle, any cancellations Stripe already applied are not undone by the 503 — `billing_cancelled:false` means "not fully verified," not "nothing at Stripe changed"; a resumed run's own readback simply runs again.*

   **OPEN GATE — unresolved this round, needs Meta-CEO re-adjudication, not builder discretion (round-6 BLOCKER, originates in ruling clause (B)):** the allowlist above (`canceled`, `incomplete_expired`) cannot be satisfied by a subscription that is `unpaid`, `paused`, or `incomplete`, because `_cancel_subscriptions` (`app/billing.py:1476`: `if s.status in ("active", "trialing", "past_due"):`) never touches those three statuses, while the readback's own `status="all"` (`app/billing.py:1475`) guarantees they are seen. A user with such a subscription fails this readback on every attempt, forever — deletion becomes permanently impossible for that user, and no retry clears it. This document does not resolve it on its own authority: either the allowlist widens to include the residual statuses `_cancel_subscriptions` never touches, or this stage cancels those residual statuses itself before reading back. **This build packet must not proceed on stage 2 until that decision lands.**
3. Delete the filesystem quota ledger for `_safe_uid(user_id)` (`engine/research_vault/download_quota.py:61,91`) — outside Postgres. Sets receipt field `quota_ledger_deleted:true` the instant this stage completes (same round 2 repair as stage 2).
4. **Capture the row PKs stage 8's purge will need — this MUST run before stage 5, because stage 5's `on delete set null` fires as part of the same statement that deletes the `auth.users` row, and after that the `user_id` link on #12/13/17 is already gone.** `select id from support_tickets where user_id=:uid` (ticket ids, so #18 can be purged by parent id even though its own row was never FK'd to `auth.users`); `select id from search_events where user_id=:uid` and `select id from analytics_events where user_id=:uid` (the tables' own bigint identity PKs, `0003_search_events.sql:38` / `0004_analytics.sql:19` — **round 2 repair for MAJOR 1: capture row PKs, never `anon_id`/`visitor_id`**, because those are device/browser identifiers a shared computer, a family, or a guest-then-registered session can spread across more than one subject; a PK captured while `user_id` still equals this subject unambiguously belongs to this subject and to no one else). Held only in request-local memory for this call — never persisted, never part of the receipt.
5. `DELETE {SUPABASE_URL}/auth/v1/admin/users/{id}` with the service-role key — the existing admin path (`lib/user_prefs.py:117-123,131-135`), named `stage="delete_identity"` in any 503. No new secret store, no new auth plane. Cascades tables #1-11,15,20,23,24 *if §4.1/§4.1b confirms it*; also fires the `on delete set null` that clears `user_id` on #12/13/17. **This stage is the point of no return only once the call itself succeeds** (round 3 fix for MAJOR 2) — every later stage is cleanup/notification, not a chance to abort; if this call errors, times out, or returns non-2xx, the response is 503 `stage="delete_identity"`, `identity_deleted:false` (see §3.1, §3.4, §3.5 test 11), because the identity was never actually deleted.
6. Update the receipt row stage 0 already inserted (`account_deletion_receipts`, keyed by `subject_digest = sha256(user_id+email)` — never the raw identifiers) to `status='completed'` (or `'failed'`), filling in `completed_at` and the per-stage results (**round-6 fix for MINOR-6**, which found this phrased as a fresh write when stage 0 already created the row).
7. Return + email the receipt as a transactional class (not suppressed by `email_prefs.marketing_opt_out`, `scripts/deploy/0007_support_email.sql:95-97`), via `app/mailer.py:send` with `template="account_deletion_confirmation"` and a deterministic `idem_key` (e.g. `f"account_deletion_confirmation:{request_id}"` — known to the caller before the call, so no post-insert row lookup is needed). This send inserts one `email_log` row exactly like any other transactional email, unique-constrained on that `idem_key` (`scripts/deploy/0007_support_email.sql:77`); stage 8 uses this same `idem_key`/`template` pair to identify exactly this row (**round-6 fix for MAJOR-4** — see stage 8 below).
8. Actions on the non-cascading stores, run LAST and on purpose, keyed on the **stage-4 captures** (never on `user_id`, which #12/13/17 no longer carry after stage 5, and never on `anon_id`/`visitor_id`, which round 2 removed as a deletion key for MAJOR 1 — see stage 4): **redact, do not delete,** `support_tickets` rows matching the captured ticket ids — `email` (`0007_support_email.sql:28`) set to a fixed placeholder, and `user_id` **explicitly set `null` by this stage's own `update`** (**round-6 fix for MAJOR-1**: the ruling's own text says stage 8 nulls `user_id`; this document does so explicitly rather than resting solely on the DDL's `on delete set null` FK, `0007_support_email.sql:29-32`, whose firing in production is unconfirmed by §4's gate — see the new Q11 clause) (replacing rounds 1-4's explicit delete of this table and of `support_ticket_messages`, which contradicted §1 item 3's own claim that #18's parent "is never deleted" — `support_ticket_messages` rows for those ticket ids are **left untouched**, `body` intact, precisely because the parent is not deleted, and need no action of their own — **round-6 correction of MAJOR-2**, which found stale text elsewhere in this document still describing #18 as "handled" here); delete `email_log` rows matching the user's `to_email` in two parts — **all rows with `created_at < requested_at`** (unchanged from round 5, historical mail predating this request), **plus every remaining row with `created_at >= requested_at` except the one row identified by the stage-7 confirmation send's own `idem_key`/`template='account_deletion_confirmation'`** (**round-6 fix for MAJOR-4**: round-5's boundary alone *retained every* row timestamped at or after `requested_at`, not only the confirmation row, so any other transactional mail this run itself triggers — e.g. a billing-cancellation notice from stage 2 — would have silently survived, undisclosed; identifying the retained row by its own unique `idem_key` instead closes that gap while leaving round-5's historical-mail boundary unchanged) — together, exactly one `email_log` row for this address survives stage 8, matching §3.4's "we keep **one** confirmation email record" precisely; that one retained row is purged later, after 30 days, by a new nightly job (**round-6 fix for MAJOR-3**, detailed just below); delete `search_events`/`analytics_events` rows matching the **captured row `id` set** (`delete from search_events where id = any(:captured_ids)`, and the same shape for `analytics_events`) — a device/browser identifier (`anon_id`/`visitor_id`) is never accepted as a deletion key because it is not provably single-subject; a captured PK is. Stage 5's cascade does not reach any of these; nothing after stage 8 can re-create them, because stage 8 runs last.

**30-day expiry of the retained confirmation row (round-6 fix for MAJOR-3 — no mechanism, owner, or test existed before this round):** the build packet implementing this spec adds one new nightly step, `scripts/purge_deletion_receipts.py`, invoked from the existing nightly pipeline (this is forward-state cleanup, not a live-request action, so per this repo's own law the nightly is its sole advancer). It runs daily: `delete from email_log where template='account_deletion_confirmation' and created_at < now() - interval '30 days'` — an index scan, not a full-table scan, via the existing `email_log_template` index (`0007_support_email.sql:89-90`). Owner: the nightly pipeline. Test: §3.5 test 13 below.
Retained on purpose, each disclosed by name in the confirmation copy and matching §3.4's copy exactly: `email_suppression` (#21, address-keyed, covers people who never registered); `ip_geo` (#14, never purged and not disclosed at all in round 1: it is not user-scoped and cannot be safely deleted by any identifier this subject uniquely owns, since IP addresses are shared across households, offices and NATs; deleting by IP would be its own cross-tenant defect, so it is retained and disclosed instead of purged); the single `email_log` (#19) row stage 7 itself writes as the deletion confirmation, identified by its own `idem_key`/`template` rather than by timestamp (**round-6 fix for MAJOR-4**), kept for 30 days as the receipt and then purged by the new nightly job (**round-6 fix for MAJOR-3**); and the user's `support_tickets`/`support_ticket_messages` rows (#17/18), kept anonymised rather than deleted, per stage 8 above (**round-5 fix for MAJOR-4**; `user_id` explicitly nulled and `email` redacted, **round-6 fix for MAJOR-1**). `stripe_events` (#16) is **not** in this disclosed list: **round-5 correction (MAJOR-2)** found it carries no per-subject column at all (§1 item 2 — no FK to `auth.users`, no payload, per `0005_user_entitlements.sql:40-44`), so there is nothing about *this* subject to disclose from it — distinct from Stripe's own external records (#27, §2.3 item 4), which round 2/3 correctly kept separate. Aggregate campaign counters (#22) are deliberately not in this list — they hold no data about *this* subject (§1 point 5: `email_campaigns` has no FK to any user), so they are not this subject's retained data and do not belong in a per-user retention disclosure.

**Why this ordering (round 2 repair for BLOCKER 1):** round 1 moved the explicit purge to run last so it would catch the receipt's own `email_log` write — correct in spirit for `email_log`, which has no FK and so is never touched by stage 5's `on delete set null`, but round 1 through 4 all purged that row along with everything older, which round-5 fixes below. Round 1 also left `support_tickets`, `search_events` and `analytics_events` looked up "for that user" via `user_id` at the final stage, and those three tables *are* FK'd with `on delete set null` (`scripts/deploy/0007_support_email.sql:32` for `support_tickets`; `0003_search_events.sql:43` and `0004_analytics.sql:32` for the other two) — stage 5's own admin-delete already nulls that column for every one of the user's rows before the final stage ever runs, so "`user_id` = this user" resolves to nothing and the purge silently no-ops. The fix is the new stage 4: capture each row's own primary key (ticket id; `search_events`/`analytics_events` row `id`) *before* stage 5 destroys the `user_id` link, and have the final stage act on those captured PKs instead — never on the device/browser identifiers those rows also carry, which round 3 confirmed are not provably single-subject (§1 item 2).

**Round-5 fix for the BLOCKER (email_log purge boundary and the receipt's own home):** rounds 1-4 bounded the `email_log` purge at `created_at <= requested_at` and stated this boundary *includes* the row stage 7 itself writes — but stage 7 (the receipt send) necessarily runs, and therefore its row is necessarily timestamped, *after* `requested_at` was captured at stage 0, so that claim was backwards: `<=` would in practice exclude the row anyway on any real clock, and the stated rationale ("still includes the row stage 7 just wrote") was simply wrong. This document instead makes the exclusion explicit and load-bearing rather than accidental: the boundary is `created_at < requested_at`, strictly before, and the confirmation row is **retained on purpose for 30 days** as the user's own proof that the deletion ran — never silently deleted with the rest of the address's mail history. A consequence, not a coincidence: because stage 8 no longer purges the very row stage 7's send created, that row's own `idem_key` unique-constraint claim (`scripts/deploy/0007_support_email.sql:77` — the DDL that defines the constraint; `app/mailer.py:181-193 _ledger_insert` is the function that hits it, not its source, **round-6 fix for MINOR-5**) survives deletion intact — a resumed/retried run's own send attempt still hits the ordinary duplicate-key protection every other transactional email relies on, with no separate "stored record" fallback needed (simplifying rounds 1-4's reasoning, which needed one only because those rounds' purge boundary destroyed this exact row).

**Round-6 refinement (MAJOR-4):** the `created_at < requested_at` boundary above scopes historical, pre-request mail — it was never meant to bound retention going *forward*, but as originally written it also retained every row timestamped at or after `requested_at`, not only the one confirmation row, so any other transactional mail this run itself triggers (e.g. a billing-cancellation notice from stage 2) would have silently survived, undisclosed, alongside it. Stage 8 (above) now additionally deletes every post-`requested_at` row except the one identified by the stage-7 confirmation's own `idem_key`/`template` — a strictly narrower, identity-based retention for that segment, leaving the historical (`created_at < requested_at`) boundary exactly as round 5 left it.

**Failure posture:** every stage idempotent/re-runnable; a failure at stage n returns 503 with `stage` + `identity_deleted` + partial receipt. A partially deleted account must never be reported as deleted, and a fully deleted identity must never be reported as still active (see §3.4). Stage 5 is the point of no return; stages 6-8 (receipt, notification, non-cascading purge) still run afterward to complete cleanup and notification even though the account is already gone.

### 3.3 Receipt fields
`request_id`, `subject_digest` (sha256 as above), `status:in_progress|completed|failed` (**round 3 addition, MAJOR 4**), `requested_at`, `completed_at`, `stages[{name,status:ok|skipped|failed,rows_deleted|null}]`, `retained[{what,why}]`, `export_offered:bool`, `billing_cancelled:bool`, `quota_ledger_deleted:bool` (round 2 addition for MAJOR 2 — read by §3.4's copy selector before `identity_deleted` is even true; **round-5 fix for MAJOR-1**: `billing_cancelled` is set `true` only once stage 2's own post-cancel **readback** confirms every subscription settled at `canceled`/`incomplete_expired` — never merely because `_cancel_subscriptions` was called and did not throw, since that function's own docstring says it never throws, `app/billing.py:1470-1482`). **Must be emitted in the K1 `EvidenceRef`/`EvidenceBlock` shape** — the builder maps these fields into that contract rather than inventing a near-miss shape. **Storage (round 2 fix for MAJOR 1):** a new macro table `account_deletion_receipts`, keyed on `request_id` with `subject_digest` indexed, deny-all RLS / service-role-only like every other administrative table in §1 — DDL is out of scope for this records packet, the build packet that implements §3 adds it, but "no new receipt row" (§3.5 test 9) now has a concrete table to assert against. **Round 3 addition (MAJOR 4 — the 409 lock):** the same table carries a partial unique index on `(subject_digest) where status='in_progress'`, written by stage 0 before any other stage runs and flipped to `completed`/`failed` at the end of the request; that row is what stage 0's insert-or-409 check (§3.2) reads.

### 3.4 Plain-language copy (EN / ZH)
Trigger: "Delete my account" / "删除我的账户". Confirm: "This removes your account and the data you created: watchlists, layouts, drawings, alerts, positions, saved scripts and chat threads. It cannot be undone. Download your data first if you want a copy." / "这将删除你的账户以及你创建的数据：自选、布局、画线、提醒、持仓、脚本和对话记录。无法撤销。如需保留副本，请先下载你的数据。" Kept-on-purpose (matching §3.2's retained-on-purpose list exactly — **round-5 fix for MAJOR-2 and MAJOR-4**: round 3's "our own payment history" claim is dropped, since `stripe_events` carries no per-subject column at all, `0005_user_entitlements.sql:40-44`; a 30-day confirmation-email line and an anonymised-support-conversation line are added): "We keep four things: a record that this email asked not to receive marketing; a network-address log that we can't selectively erase because addresses are often shared with other people; a confirmation-email record, kept for 30 days as proof the deletion ran; and your support conversations, kept in anonymised form for our records." (**round-6 fix for MINOR-4**, which found "we keep one confirmation email record for 30 days…" a verb clause breaking the noun-phrase list every other item follows) / "我们会保留四项：该邮箱不再接收营销邮件的记录；一份网络地址日志——由于该地址可能与他人共用，我们无法只删除你这一部分；一封确认邮件的记录，会保留 30 天作为本次删除已执行的凭证；以及你的客服对话，会以匿名形式保留作为我们的记录。" Typed confirm label: "Type your email address to confirm" / "请输入你的邮箱以确认". Success: "Your account is deleted." / "你的账户已删除。" plus "We sent a confirmation to <email>." — never "request submitted" when complete, never "deleted" when it isn't.

**Partial failure — three copies, chosen by side-effects-taken, not by `identity_deleted` alone (round 2 repair for MAJOR 2; branching on `identity_deleted` alone was wrong because stages 2-3 take irreversible action — a cancelled Stripe subscription, a wiped download-quota ledger — before stage 5 even runs, and the round-1 copy told a user in exactly that state "nothing was lost"). The receipt gains two more boolean fields, `billing_cancelled` (set only once stage 2's own readback confirms cancellation, **round-5 fix for MAJOR-1** — see §3.2 stage 2, §3.3) and `quota_ledger_deleted` (set the instant stage 3 completes), both settled before stage 5, so a 503 anywhere at stage 2-4 still carries an accurate value; the client picks copy off these plus `identity_deleted`, never off `identity_deleted` in isolation:**
- `identity_deleted:false`, `billing_cancelled:false`, `quota_ledger_deleted:false` (failure at stage 0-1, before any side effect): "We could not finish. Your account is still active and nothing was lost. Contact support with this reference: <request_id>." / "未能完成。你的账户仍然有效，数据没有丢失。请凭此编号联系客服：<request_id>。"
- `identity_deleted:false` with `billing_cancelled:true` and/or `quota_ledger_deleted:true` (failure at stage 2-4, **or stage 5 itself failing to complete — round 3 fix for MAJOR 2, which found a stage-5 call that errors, times out, or returns non-2xx still mapped to the "deleted" copy below; the point of no return is stage 5 *succeeding*, not stage 5 being attempted, see §3.1/§3.2 and §3.5 test 11**): "We could not finish, and some steps already happened: [your subscription was cancelled] [your download history was cleared] (only the ones that already ran are named). Your account itself is still active. Contact support with this reference: <request_id>." / "未能完成，但部分步骤已经生效：[你的订阅已取消][你的下载记录已清除]（仅列出已执行的步骤）。你的账户本身仍然有效。请凭此编号联系客服：<request_id>。"
- `identity_deleted:true` (failure at stage 6-8, strictly after stage 5 has **completed successfully** — the point of no return): "Your account has been deleted. We're still finishing some cleanup, but there's nothing you need to do. Contact support if you have questions, reference: <request_id>." / "你的账户已删除。我们仍在完成部分清理工作，你无需进行任何操作。如有疑问请联系客服，编号：<request_id>。"
Never show the "nothing was lost" copy once `billing_cancelled` or `quota_ledger_deleted` is true — those are exactly the irreversible actions MAJOR 2 named — and never show either "still active" copy once `identity_deleted` is true, the inversion BLOCKER 2 named. A stage-5 call that itself fails is always `identity_deleted:false`, never `true` — round 3's MAJOR 2 fix.

### 3.5 Deletion — not done unless (macro `tests/test_account_deletion.py`)
1. Cascade proof per table: seeded user with ≥1 row in every §1 row 1-11/15/20/23/24 → after stage 5 (identity deletion), each returns 0 rows for that id, failing per table name.
2. Non-cascading stores handled correctly AFTER the full run (stage 8, which runs after the stage-7 receipt send) — **round-6 rewrite, extending round-5's fix for the BLOCKER with MAJOR-4's identity-based retention**: seed the address with (i) an ordinary pre-request `email_log` row (`created_at < requested_at`), (ii) a second, *non-confirmation* row timestamped at or after `requested_at` (simulating stage 2's own billing-cancellation mail — the exact gap MAJOR-4 named), and (iii) the stage-7 confirmation row itself; after stage 8, only the confirmation row (matched by its own `idem_key`/`template`) is present and unredacted, as the 30-day receipt — both (i) and (ii) are gone; `search_events`/`analytics_events` hold no row matching the captured row `id` set from stage 4 — the test that would have caught #12/13's now-nulled `user_id`, which the round-1 ordering (querying by `user_id` at the final stage) could never pass; `support_tickets` rows matching the captured ticket ids still exist, with `user_id` **explicitly null via stage 8's own `update`** (not merely assumed from the FK — **round-6 fix for MAJOR-1**) and `email` redacted to the fixed placeholder; `support_ticket_messages` rows for those ticket ids are byte-identical to before the run, `body` intact.
3. Stage-4 capture actually runs before stage 5, not after: seed a user, run the request up through stage 4, snapshot the captured-identifier set, then null `auth.users`-linked `user_id` on #12/13/17 directly (simulating stage 5's cascade) before stage 8 runs — confirm stage 8 still finds and (for `search_events`/`analytics_events`) removes, and (for `support_tickets`) redacts, every row using the pre-captured set, proving the capture (not a live `user_id` lookup) is what stage 8 actually uses for all three (**round-5 update**: `support_tickets` is now redacted, not removed, so this test asserts a redact, not a delete, for that table).
4. Retention intentional and asserted (**round-6 update**: fixes round-5's "four" miscount and switches the confirmation row's identification from timestamp to identity): `email_suppression` for that address, the `ip_geo` row for any IP the user's session used, the single retained `email_log` confirmation row (present, identified by its own `idem_key`/`template`, not by timestamp — **round-6 fix for MAJOR-4**), and the user's `support_tickets`/`support_ticket_messages` rows (present but `email` redacted and `user_id` explicitly null on the former — **round-6 fix for MAJOR-1** — `body` untouched on the latter) are all still present after deletion; receipt's `retained[]` names **five tables across four retention groups** (`email_suppression`, `ip_geo`, `email_log`, and `support_tickets`+`support_ticket_messages` as one group) — **round-6 fix for MINOR-7**, which found the fourth group is two tables, not one.
5. Cross-tenant safety: a second seeded user's rows in every table byte-identical before/after.
5b. **Shared-device cross-tenant safety (round 2 addition for MAJOR 1 — the gap test 5 alone cannot catch):** seed user A and user B with `search_events`/`analytics_events` rows sharing the *same* `anon_id`/`visitor_id` (the family/office/guest-then-registered-account scenario the finding names) but distinct row `id`s; delete user A; assert every row keyed to B's captured ids still exists, byte-identical, and that stage 8's delete statement was scoped to A's captured `id` set, never to the shared `anon_id`/`visitor_id` value.
6. Wrong confirmation deletes nothing: mismatch → 400, zero writes anywhere (row counts, not mock-call counts).
7. Stripe first (**round-5 rewrite for MAJOR-1**: `app/billing.py:1470-1482 _cancel_subscriptions` never raises, so a real test cannot inject an exception from it — the failure must come from the readback): seed a subscription that remains `active` (or `trialing`/`past_due`) when stage 2's post-cancel readback re-lists it → 503 `stage="cancel_subscriptions"`, `identity_deleted:false`, `billing_cancelled:false` (the readback found an unsettled subscription, so nothing is asserted as done), `auth.users` still holds the user (stage numbering unchanged — Stripe cancellation is still stage 2); the client-facing copy asserted is the tier-1 "still active, nothing was lost" text, since no side effect actually completed — never the tier-2 "some steps already happened" text this round retires for this specific test.
8. Post-point-of-no-return failure messaging (BLOCKER 2): injected failure at stage 8 → 503 `stage="purge_noncascading"`, `identity_deleted:true`; the client-facing copy asserted is the "account has been deleted, cleanup still finishing" text, never the "still active" text.
9. Replay after completion is a 401, not a 200 (MAJOR 1): re-run the same request with the same token after a completed deletion → 401, because the token's user id no longer exists (§3.1's ordinary auth contract, not a special replay path) — no additional deletion side effects; no duplicate receipt email, because the original `email_log` row's `idem_key` unique constraint (`scripts/deploy/0007_support_email.sql:77`, hit via `app/mailer.py:181-193 _ledger_insert` — **round-6 fix for MINOR-5**, correcting a citation of the function that hits the constraint in place of the DDL that defines it) is still present — **round-5 fix**: stage 8 no longer purges that row (it is retained for 30 days, see §3.2 stage 8), so the ordinary duplicate-key guard every transactional email relies on still applies; no separate stored-record check is needed.
10. Receipt carries no raw identifier: neither email nor user id in any stored field; `subject_digest` reproduces from `sha256(user_id+email)`.
11. **Stage-5-itself failure (round 3 addition, MAJOR 2):** injected failure inside the admin `DELETE` call (error/timeout/non-2xx) → 503 `stage="delete_identity"`, `identity_deleted:false` (not `true` — the call did not complete); the client-facing copy asserted is the "some steps already happened / still active" text, never "Your account has been deleted"; `auth.users` still holds the row.
12. **409 concurrency (round 3 addition, MAJOR 4):** two requests for the same account issued back to back — the first inserts the stage-0 in-progress marker (`status='in_progress'`) and proceeds; the second, while that marker still holds, gets 409 with zero writes beyond its own failed insert attempt; after the first completes (`status='completed'` or `'failed'`), a third request is accepted normally.
13. **30-day expiry of the retained confirmation row (round-6 addition, MAJOR-3):** seed an `email_log` row with `template='account_deletion_confirmation'` and `created_at` 31 days in the past → the nightly purge job (`scripts/purge_deletion_receipts.py`) removes it; a second such row at 29 days old survives (boundary); a third row of a *different* `template` at 31 days old also survives (the job only ever touches this one template).

---

## 4. Production readbacks the Meta-CEO must run BEFORE any build (mandatory gate)

Via the Management API with the PAT in `charting-app/.env` (`supabase/migrations/README.md:31-35`; strip `--` comments, use `curl` not python-urllib, `README.md:37-39`). Project ref `fsldfzlxyavsuwqbceod` (`README.md:4`). Every §1 cascade cell is unproven until Q1/Q1b answer.

1. **Q1 (most important):** `select conrelid::regclass as tbl, conname, confdeltype from pg_constraint where confrelid='auth.users'::regclass order by 1;` — compare row-for-row vs §1's *direct* cascades (#1,2,4,5,6,7,8,9,11,15,20,23,24). **Known-expected absences, do not treat as a gate failure:** #3 (`watchlist_symbols`), #10 (`brain_messages`) and #18 (`support_ticket_messages`) FK to a *public* parent (`watchlists`, `brain_threads`, `support_tickets` respectively), never straight to `auth.users`, so Q1 cannot and should not list them — verify them with Q1b instead. Any *other* §1 direct-cascade table absent from Q1 is a real gate failure.

   **Q1b (two-level walk, closes the gap Q1 structurally cannot cover — round 3 fix for MINOR 4, whose leading `1b.` rendered as loose body text rather than a list item):** `select conrelid::regclass as tbl, conname, confdeltype from pg_constraint where confrelid in (select conrelid from pg_constraint where confrelid='auth.users'::regclass and confdeltype='c') order by 1;` — for every parent that Q1 already confirmed cascades, list what cascades off *it*. Expect `watchlist_symbols`→`watchlists` (#3) and `brain_messages`→`brain_threads` (#10) here, proving they ARE cleared when stage 5 runs, transitively. **`support_ticket_messages` (#18) must NOT appear here**, because its parent `support_tickets` (#17) uses `set null`, not cascade, so it is excluded from Q1b's `confdeltype='c'` filter on the parent side — #18 correctly needs no action anywhere in this document (**round-6 correction of MAJOR-2**, replacing a stale claim that stage 8 "explicitly deletes" it — stage 8 does not touch #18 at all, see §3.2 stage 8). If Q1b ever lists #18, the gate fails the other way: the DDL changed and #17 now cascades, #18 would already be gone by the time stage 8 runs, and §3.2's current stance that #18 needs no action becomes wrong, not merely redundant — #17's redact-not-delete design (§3.2 stage 8) would also need reconsidering, since its parent would no longer survive to be redacted.
2. **Q2:** `select table_name from information_schema.tables where table_schema='public' order by 1;` — tables in prod but no file (README:41-53 makes this non-optional).
3. **Q3:** `select relname, relrowsecurity, relforcerowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public';` — RLS actually on.
4. **Q4:** `select tablename, policyname, cmd, qual from pg_policies where schemaname='public' order by 1,2;` — confirms deny-all posture of #12/13/17-22 and the `scripts_public_read` exception (#5).
5. **Q5:** the column *names* are already verified (`0007_portfolio_positions.sql:40-43` "VERIFIED PRESENT"); what is genuinely unknown is the FK/PK/NOT NULL/defaults/index, so query those instead: `select conname, contype, confrelid::regclass, confdeltype from pg_constraint where conrelid='public.portfolio_positions'::regclass;` (confirms the row 11 FK-to-`auth.users` claim, currently unverified per `0007_portfolio_positions.sql:60-64` "NOT ANONYMOUSLY INTROSPECTABLE") plus `select column_name, is_nullable, column_default from information_schema.columns where table_schema='public' and table_name='portfolio_positions';` (NOT NULL + defaults).
6. **Q6:** Q1's query restricted to `nspname='auth'` — do `auth.identities`/`sessions`/`refresh_tokens`/`mfa_factors`/`one_time_tokens` cascade? Documented Supabase behaviour, not a fact in our files.
7. **Q7:** `select tgname, tgtype, tgenabled from pg_trigger where tgrelid='auth.users'::regclass and not tgisinternal;` — `0001_init.sql:24-25` installs `on_auth_user_created`; confirm no delete-side trigger.
8. **Q8:** `select id, name, public from storage.buckets;` and, if any, `select count(*) from storage.objects where owner='<test uid>';` — no file in either repo creates a bucket, so whether user files exist is genuinely unknown.
9. **Q9:** `select table_name, privilege_type, grantee from information_schema.role_table_grants where table_schema='public' and grantee in ('anon','authenticated') order by 1;` — confirm `profiles` UPDATE stays narrowed to `display_name` (`0003:27`, `0006:50`).
10. **Q10 (the only proof of hard-vs-soft delete):** create a throwaway user, seed one row per §1 table, snapshot counts — **and, before running the delete, also snapshot the row-level PKs §3.2 stage 4 will need** (`support_tickets.id`, `search_events.id`, `analytics_events.id` — round 3 fix: these are the tables' own bigint identity PKs, `0003_search_events.sql:38` / `0004_analytics.sql:19`, never `anon_id`/`visitor_id`, which round 2/3 removed as a deletion key) — then run `DELETE /auth/v1/admin/users/{id}` and re-snapshot. Record whether the row vanishes from `auth.users` or is merely marked deleted.
11. **Q11 (orphan census after Q10, round 2 fix; round-5 updates the support/email portion for MAJOR-4 and the BLOCKER; round-6 adds the `user_id`-nullity check and updates the email_log portion for MAJOR-1/MAJOR-4):** using the row `id`s *captured before* Q10's delete — never `user_id`, which the `on delete set null` FKs on #12/13/17 will already have cleared, and never `anon_id`/`visitor_id` — count rows in `search_events`/`analytics_events` still bearing those captured `id`s (the exact size of §3.2 stage 8's delete job for those two tables); count `support_tickets` rows by captured ticket id (the exact size of stage 8's **redact** job — these rows are not deleted, so a non-zero count here is expected and correct, unlike the other two) **and separately confirm every one of those rows has `user_id is null`** (**round-6 addition for MAJOR-1**: this is the actual proof the anonymisation guarantee needs — without it, the guarantee rests solely on the FK's unconfirmed firing; stage 8's own explicit `update ... set user_id = null` should make this true regardless of the FK, and this query is what confirms it against production instead of asserting it from the file alone); confirm `support_ticket_messages` rows for those ticket ids are unchanged in count (stage 8 never touches them); and, for `email_log`, confirm that after Q10's delete exactly one row remains for that `to_email`, matching `template='account_deletion_confirmation'`, and every other row for that address is gone (**round-6 update for MAJOR-4**, replacing a timestamp-only count that could not detect a surviving non-confirmation row created at or after `requested_at`). Querying by `user_id` after the fact would reproduce BLOCKER 1's defect inside the readback plan itself.

**Gate rule:** if Q1/Q1b disagree with §1 for any table (excluding the known-expected #3/#10/#18 absences named above), §3.2 stage 8's explicit-delete list grows and this document is amended before code is written — the builder does not reconcile the difference on the fly.

---

## 5. Standing-gate compliance
- **No third nav family:** the future UI is a section of the existing `M templates/account.js`, loaded via `M templates/theme.js:469-480`; no new header/route chrome. This packet ships none of it.
- **No second auth/tenant plane, no secret store** (F12 do_not_redo): identity is existing Bearer/Supabase verification, the privileged call is the already-shipped GoTrue admin URL builder `lib/user_prefs.py:131-135`, and the Terminal stays a keyless proxy.
- **Nulls printed, never hidden:** §2.3 exclusions, §2.2 `unavailable`, §4 Q6/Q8/Q10 named as unknown, CSV named as deferred.
- **No LLM-originated signals/scores/escalations; no trading authority.** Export = stored rows; deletion = mechanical.
- **No proprietary Market Ontology code/text/data/assets** reproduced here.
- **Corrections are typed states:** `ok|skipped|failed` per stage; 503+`stage` for partial; never a bare boolean.
- **Verification law** (`terminal/AGENTS.md:42-43`): the *build* packet that implements this owes a fresh incognito end-to-end pass and light+dark+zh crops of the account panel. This records packet owes none — it renders nothing.

## 6. Open items this round could not close (nulls printed, not hidden)
Two items from the round-6 review remain open. Neither is closed by builder discretion — both need a Meta-CEO decision or act, not another guess at the design:
1. **Stage 2's Stripe readback allowlist (BLOCKER, round-6):** flagged in place at §3.2 stage 2 above. `_cancel_subscriptions` (`app/billing.py:1476`) never touches `unpaid`/`paused`/`incomplete` subscriptions, while the readback's allowlist (`canceled`, `incomplete_expired`) requires every subscription to have settled at one of those two — a user with a residual-status subscription can never pass the readback, and deletion becomes permanently impossible for them. This is a defect in the ruling's own construction (clause B), not in how this round executed it. Repair needs a Meta-CEO choice between widening the allowlist and having stage 2 cancel the residual statuses itself before reading back; this document does not pick one.
2. **Round-4's MAJOR-5, MAJOR-6, and 4 MINORS (MAJOR-6, round-6):** the round-4 review (head `62e406dd`) named six additional findings beyond the BLOCKER and MAJORS 1-4 the Meta-CEO ruling closed. No file preserving round-4's review text for that head exists in the review archive (`terminal_reviews/`, checked: 435, 445, 446, 484, 490, 502, 507, 508, 512, 513 only — no `62e406dd`-headed file). This document cannot close findings it cannot read; they need the Meta-CEO to re-issue or re-derive them from a fresh review pass, not a builder guess at what they might have said.

## 7. Live proof
**Records only — the merged path is the proof.** No live URL, no render, no deployment, no screenshot. The proof of this packet is the merged file plus the §4 readback results appended to it in a later build packet.
