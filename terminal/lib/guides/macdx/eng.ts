// Guide content for the macdx/eng premium-suite module — bundled (NOT served from public/):
// Next's standalone server does not serve public/ files added after the build, and guides
// ship with the build rather than being rsync'd like /data. Lazily imported by GuidePanel.
// Markdown is rendered through lib/md.ts (escape-safe).

export const en = "# MACD Engine\n\nA MACD rescaled so that a number means something. The curve is bounded, the rails are overbought and oversold, and 80 on one symbol is 80 on every other.\n\n## What you see\n\nA thick curve travelling through a pane whose useful range is +100 to -100, with a zero line in the middle. That bounding is the whole idea. A raw MACD is measured in price units, so its scale depends on the symbol and the era and no fixed threshold means anything; here each bar is rescaled against the strongest momentum of the trailing 250 bars. +100 is the strongest up-momentum of roughly the last year of bars, -100 the strongest down-momentum, and the vendor's signature flat line hugging a rail at saturation falls straight out of that.\n\nAbove +100 and below -100 sit two translucent strips — the overbought and oversold caps the saturated curve clamps flat against.\n\nThe thin companion in the warning hue is the signal line, smoothed on the *normalised* series so both share one scale.\n\nIn the default **HeatMap** colouring the curve tells you how extreme it is, not which way it points:\n\n- **Under 40 in absolute terms** — muted. Noise; the pane says so before you read the level.\n- **Between 40 and 80** — the brand accent. The working range.\n- **Beyond 80** — the directional *opportunity* hue: deep oversold reads with the up colour, deep overbought with the down colour. Deliberately not the sign of the value.\n\nSwitch to **Rising / Falling** and the curve is coloured by slope instead, which is the familiar reading if the heat map fights your instincts.\n\n## How to trade it\n\nZero is the regime line and the rails are the stretch marks. The conservative use is to let this module set your bias and let something else time the entry: above zero you are looking for longs, below zero for shorts, and near a rail you are looking for the *end* of the current push rather than the start of a new one.\n\nThe version worth having: wait for the curve to saturate against a rail, then take the entry only when it leaves — the extreme itself can persist far longer than a position can. That is exactly the trade the Signals module automates.\n\n**When not to use it:** in a range-bound, low-volatility market. The curve will cross zero repeatedly and every crossing will be a small loss.\n\n## Settings\n\n**Fast Length** — bars in the fast leg, from 2 to 50, default 10.\n\n**Slow Length** — bars in the slow leg, from 5 to 100, default 20. Keep it above the fast length or the curve inverts.\n\n**Signal Length** — the smoothing applied to the normalised MACD to draw the signal line, from 2 to 50, default 9.\n\nThe 10/20/9 default is faster than the textbook 12/26/9 on purpose; raise all three for swing charts.\n\n**MACD MA Type** — EMA or SMA, the average used for the fast and slow legs. Default EMA.\n\n**Signal MA Type** — EMA or SMA, the average used for the signal line. Default EMA.\n\n**Color Mode** — HeatMap (colour by how extreme the value is) or Rising / Falling (colour by slope). Default HeatMap.\n\nThe 250-bar normalisation window and the ±100 rails are fixed for the whole suite, so every module in this pane agrees about what \"extreme\" means.\n\n**Shared suite settings:** the five satellite modules — Signals, Histogram, Divergences, Phase Trend and MTF Dashboard — read these live MACD settings. Retuning the lengths or MA types here therefore updates their calculations too; all six modules stay on the same MACD rather than silently falling back to defaults.\n\n## Signals & alerts\n\nOne chart event comes from the engine itself: **macdx_zero_cross**, emitted when the curve crosses zero — the headline regime change. Direction is on the event, strength is the absolute reading at the crossing, and the label quotes the value, as in `MACD crossed above zero · 12.4`.\n\n**macdx_zero_cross is chart-only today:** it appears in the chart event stream but is not a condition you can select in Alert Center. Entering or leaving an extreme strip does *not* emit an engine event; that reading belongs to the Signals module, which gates it on a signal-line cross as well. Chart events are evaluated on closed bars and a confirmed crossing is never rewritten when more history loads.\n";
export const zh = "# MACD 引擎（MACD Engine）\n\n一条重新标定过的 MACD，让数字本身有意义。曲线有界，轨道即超买超卖，某只股票上的 80 与其他任何一只上的 80 含义相同。\n\n## 图上看到什么\n\n一条较粗的曲线在有效区间 +100 到 -100 的窗口里行进，中间是零轴。\"有界\"正是全部要点。原始 MACD 以价格为单位，其量纲取决于标的与年代，任何固定阈值都毫无意义；这里每一根都以过去 250 根内最强的动能为基准重新标定。+100 表示大约最近一年K线里最强的上行动能，-100 则是最强的下行动能——那种在饱和处贴着轨道走平的经典形态，正是由此自然产生的。\n\n+100 之上与 -100 之下各有一条半透明色带，就是饱和曲线压平其上的超买与超卖顶盖。\n\n警示色的细线是信号线，它是在**归一化后**的序列上平滑得到的，因此两者共用同一把尺子。\n\n在默认的 **HeatMap** 配色下，曲线颜色表示它有多极端，而不是它指向哪边：\n\n- **绝对值低于 40**——暗色。噪音；窗口在你读数值之前就说明了。\n- **40 到 80 之间**——品牌强调色，也就是有效工作区间。\n- **超过 80**——方向性的**机会**色：深度超卖用上涨色，深度超买用下跌色。刻意不采用数值本身的正负号。\n\n切换到 **Rising / Falling** 则改为按斜率上色；如果热力配色与你的直觉打架，这是那个更熟悉的读法。\n\n## 怎么用它交易\n\n零轴是状态线，轨道是拉伸痕。保守用法是让这个模块决定你的偏向，让别的东西负责择时：零轴之上找多，零轴之下找空，贴近轨道时找的是当前这一推的**结束**，而不是新一推的开始。\n\n真正值得用的版本：等曲线压在轨道上饱和，然后只在它离开时入场——极值本身能持续的时间，往往远超一个仓位能承受的时间。这正是信号模块替你自动化的那笔交易。\n\n**什么时候不要用：** 区间震荡、低波动的市场。曲线会反复穿越零轴，而每一次穿越都是一笔小亏。\n\n## 设置项\n\n**Fast Length 快线周期**——快腿的K线数，可设 2 到 50，默认 10。\n\n**Slow Length 慢线周期**——慢腿的K线数，可设 5 到 100，默认 20。务必大于快线周期，否则曲线会反相。\n\n**Signal Length 信号线周期**——对归一化 MACD 所做的平滑，用于绘制信号线，可设 2 到 50，默认 9。\n\n10/20/9 的默认值刻意比教科书的 12/26/9 更快；做波段图时三者一并调高。\n\n**MACD MA Type MACD 均线类型**——EMA 或 SMA，用于快慢两条腿，默认 EMA。\n\n**Signal MA Type 信号线均线类型**——EMA 或 SMA，用于信号线，默认 EMA。\n\n**Color Mode 配色模式**——HeatMap（按数值有多极端上色）或 Rising / Falling（按斜率上色），默认 HeatMap。\n\n250 根的归一化窗口与 ±100 轨道对整个套件是固定的，以保证同一窗口内每个模块对\"极端\"的理解一致。\n\n**套件共享设置：** 五个卫星模块——信号、柱状图、背离、阶段趋势与多周期面板——都会实时读取这里的 MACD 参数。因此在这里调整周期或均线类型，也会同步更新它们的计算；全部六个模块始终描述同一条 MACD，不会悄悄退回默认值。\n\n## 信号与提醒\n\n引擎本身只产生一类图表事件：**macdx_zero_cross**，在曲线穿越零轴时发出，这是头牌状态切换。方向写在事件上，强度取穿越处的绝对读数，文案会带上数值，例如 `MACD 上穿零轴 · 12.4`。\n\n**macdx_zero_cross 目前仅用于图表：** 它会进入图表事件流，但不能在提醒中心选作条件。进入或离开极值色带在这里**不会**产生引擎事件；那个读数归信号模块管，而且它还额外要求同时发生信号线交叉。图表事件在收盘K线上评估，已确认的穿越不会因加载更多历史而被改写。\n";
