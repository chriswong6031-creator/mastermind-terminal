// Guide content for the macdx/mtf premium-suite module — bundled (NOT served from public/):
// Next's standalone server does not serve public/ files added after the build, and guides
// ship with the build rather than being rsync'd like /data. Lazily imported by GuidePanel.
// Markdown is rendered through lib/md.ts (escape-safe).

export const en = "# MTF Dashboard\n\nThe same normalized MACD at three depths, in one corner table — the fastest way to tell a real turn from a one-grouping wobble.\n\n## What you see\n\nThree columns show **Chart**, **2×** and **4×**. Three rows show **MACD** (the current normalized reading, slope and heat colour), **Signal** (the latest extreme-zone MACD/signal-line cross and its age) and **Phase** (the committed bullish or bearish phase and how long it has held). Recent Signal cells are brightest; older in-window crosses fade while Phase remains a persistent regime read.\n\n**How the coarse columns are built — worth reading once.** Nothing is fetched from an independent timeframe feed. The 2× and 4× columns group the bars already loaded on your chart — first open, highest high, lowest low, last close and summed volume — then recompute MACD on each shorter series. Groups are counted in bars, not clock time, so on a daily chart they are 2-day and 4-day blocks rather than weekly and monthly candles. The newest coarse group is excluded until complete, so 4× can sit up to three chart bars behind and only changes when a full group closes. It also has one quarter as many observations, so the normalized 10/20/9 default needs deeper loaded history to settle there.\n\nThat deliberate lag is the non-repaint rule made visible: a completed 2× or 4× cell is never revised by an in-progress aggregate.\n\n## How to trade it\n\nRead the columns as a sequence, not a score. A turn worth trading often appears on Chart first, then spreads to 2× and finally 4×. Each step adds confirmation while surrendering some of the move; choose the stage that matches your holding period.\n\nPhase is the strongest filter. A fresh Chart Signal while 4× remains in the opposite Phase is a countertrend trade. The same Signal with 4× already committed in its direction is continuation. A MACD reading that is still positive but slopes down is early deterioration, not yet a bearish phase by itself.\n\n## Settings\n\n**Position** — which corner of the pane holds the table.\n\n**Compact** — tightens row height, padding and type for a short pane.\n\nThose are the module's only two controls. The dashboard has no row toggles, column toggles or recency-fade setting. It reads the MACD Engine's live lengths and MA types, plus the Signals module's live extreme threshold, so the dashboard stays calibrated to the rest of MACD Ultimate.\n\n## Signals & alerts\n\nThis dashboard emits no alert events. Its cells summarize MACD state, extreme-zone crosses and phase for the three resampled columns; create alerts from the MACD Signals, Histogram or Divergences modules that own alertable event types.\n\nThe coarse columns use complete fixed bar groups only. Within one loaded chart the result is deterministic, and no completed dashboard cell repaints.\n";
export const zh = "# 多周期面板（MTF Dashboard）\n\n同一条归一化 MACD，同时看三种深度，收进角落里的一张表——想分清真正转向与单一分组的一次晃动，这是最快的办法。\n\n## 图上看到什么\n\n三列依次是 **Chart（图表）**、**2×** 与 **4×**。三行依次是 **MACD**，显示当前归一化读数、斜率与热力配色；**Signal（信号）**，显示最近一次极值区 MACD/信号线交叉及距今多久；**Phase（阶段）**，显示已经确立的多头或空头阶段以及持续了多久。刚发生的 Signal 最亮，仍在有效窗口内的旧交叉会逐渐变淡，而 Phase 始终是持续性的状态读数。\n\n**粗列是怎么来的——请务必读一遍。** 这里不读取独立周期行情源。2× 与 4× 列把图上已经加载的K线分组——第一根开盘、区间最高价、区间最低价、最后一根收盘与成交量合计——再在每条更短的序列上重算 MACD。分组按**根数**切，不按时钟切，因此在日线图上它们是两日与四日分组，不是周线与月线。最新粗分组在凑满之前会被剔除，所以 4× 最多可能落后图表三根K线，并且只在完整分组收盘后更新。它的观察数量也只有原来的四分之一，因此默认的归一化 10/20/9 MACD 在那里需要更深的已加载历史才能稳定。\n\n这种刻意的滞后，就是不重绘规则的可见结果：已经完成的 2× 或 4× 单元格不会被仍在形成的分组改写。\n\n## 怎么用它交易\n\n把三列读成一个先后顺序，而不是一个总分。值得做的转向通常先出现在 Chart，随后传到 2×，最后才轮到 4×。每往后一步都增加确认，也会让掉一段行情；选择与你持仓周期相称的阶段。\n\nPhase 是最强的过滤器。4× 仍处于相反 Phase 时，新出现的 Chart Signal 属于逆势交易；4× 已经向同一方向确立时，同一个 Signal 才是顺势延续。MACD 数值仍为正但斜率向下，只是早期恶化，本身还不等于空头阶段。\n\n## 设置项\n\n**Position 位置**——表格停靠在子窗口的哪个角。\n\n**Compact 紧凑模式**——缩短行高、内边距与字号，适合较矮的子窗口。\n\n本模块只有这两个控件，没有行开关、列开关或时效淡化设置。面板会读取 MACD 引擎当前的周期与均线类型，以及信号模块当前的极值门槛，因此它和 MACD Ultimate 其他模块始终使用同一套标定。\n\n## 信号与提醒\n\n本面板不会产生提醒事件。各单元格只是汇总三个重采样列上的 MACD 状态、极值区交叉与阶段；如需提醒，请到真正拥有可提醒事件的 MACD 信号、柱状图或背离模块中创建。\n\n粗列只使用完整的固定K线分组。就单张已加载的图而言，结果完全确定，已经完成的面板单元格不会重绘。\n";
