// Guide content for the macdx/mtf premium-suite module — bundled (NOT served from public/):
// Next's standalone server does not serve public/ files added after the build, and guides
// ship with the build rather than being rsync'd like /data. Lazily imported by GuidePanel.
// Markdown is rendered through lib/md.ts (escape-safe).

export const en = "# MTF Dashboard\n\nThe same normalized MACD at three depths, in one corner table — the fastest way to tell a real turn from a one-timeframe wobble.\n\n## What you see\n\nThree rows: your chart's bars, then the same bars in groups of two, then in groups of four. Across each row — **MACD** (the normalized −100 to +100 reading), **vs Signal** (above or below its signal line), **Histogram** (its sign, and rising or falling) and **Phase** (the committed up or down phase, the state the Phase Trend squares draw). Cells fade as their state ages; a bright cell is a fresh change.\n\n**How the coarse rows are built — worth reading once.** Nothing is fetched. The ×2 and ×4 rows are the bars already on your chart, grouped: first open, highest high, lowest low, last close, volume summed, with the MACD recomputed on that shorter series. Three things follow. Groups are counted in bars, not clock time, so on a daily chart the coarse rows are 2-day and 4-day candles rather than the week and the month. The newest group is excluded until it is complete, so the ×4 row can sit up to three bars behind the chart and only moves when a group closes. And a coarse row inherits the loaded history — the ×4 row has a quarter of the bars, and a 10/20/9 MACD needs plenty of them to settle.\n\nIn exchange, the table can never disagree with your pane over how a vendor built its higher timeframe, and no row repaints when a late higher-timeframe bar lands.\n\n## How to trade it\n\nRead the rows as a sequence, not a score. A turn worth trading usually starts on the base row, spreads to ×2 a few bars later, then ×4 — each step confirmation, each costing you some of the move. Pick the step matching your holding period and stay with it.\n\nThe phase column is the best filter. A base-row cross while ×4 is still in the opposite phase is a countertrend trade, whatever the histogram says; the same cross with ×4 already turned is continuation. Extreme-zone crosses agreeing on two rows at once are the rarest and best version.\n\nA histogram cell disagreeing with its own MACD cell — falling while the value is still positive — is the early half of a turn — the reason to watch a table rather than one line.\n\n## Settings\n\n**Position** — which corner of the pane holds the table. **Compact** tightens padding and type.\n\n**MACD / vs Signal / Histogram / Phase** — one toggle per column.\n\n**Recency Fade** — the age dimming, on by default.\n\nThe MACD is not configured here. Every row uses MACD Engine's **Fast Length**, **Slow Length**, **Signal Length** and both MA types exactly as you have them, so the table and the curve below it always describe the same indicator.\n\n## Signals & alerts\n\nOne event: **macdx_mtf_align**, fired when all three rows agree on a phase they did not agree on before; direction on the event, base-row absolute MACD as strength. Crosses, histogram flips and divergences belong to the modules that produce them — alert there.\n\nGroups are cut by bar count and the unfinished newest one is dropped, so a deeper history can slice them differently. Within one loaded chart the table is fully deterministic and no cell rewrites itself.\n";
export const zh = "# 多周期面板（MTF Dashboard）\n\n同一条归一化 MACD，三种粗细的K线，收进角落里的一张表——想分清\"真转向\"和\"只在一个周期上晃了一下\"，这是最快的办法。\n\n## 图上看到什么\n\n三行：你图上的原始K线，然后是两根一组、四根一组的同一批K线。每行横向依次是 **MACD**（归一化到 −100 至 +100 的读数）、**vs Signal**（在信号线之上还是之下）、**Histogram**（柱子的正负，以及在放大还是缩小）与 **Phase**（已确立的多头或空头阶段，与阶段趋势方块画的是同一个状态）。格子随状态变旧而淡化，亮的就是刚发生的变化。\n\n**粗行是怎么来的——请务必读一遍。** 这里不向服务器多取任何数据。×2 与 ×4 两行就是你图上已有的K线合并出来的：第一根的开盘、区间最高价、区间最低价、最后一根的收盘、成交量相加，再在这条更短的序列上重算 MACD。由此有三个后果。分组按**根数**切，不按时钟切，所以在日线图上，粗行是\"两日线\"和\"四日线\"，而不是周线和月线。最新一组在凑满之前会被剔除，因此 ×4 行可能比图面慢最多三根K线，并且只在一组走完时才动。粗行继承已加载的历史——×4 行只有四分之一的K线，而 10/20/9 的 MACD 需要相当数量的K线才能稳定下来。\n\n换来的是：这张表不会因为哪家数据商的大周期拼法不同而与你的子窗口打架，也不会因为一根迟到的大周期K线而改写已显示的状态。\n\n## 怎么用它交易\n\n把三行读成一个**先后顺序**，而不是一个总分。值得做的转向通常从基础行开始，过几根传到 ×2，再往后才轮到 ×4——每往前一步都是一次可以下手的确认，同时也让掉一段行情。挑一个与你持仓周期相称的台阶，然后守住它。\n\n这里最好用的过滤器是阶段列。×4 还处在相反阶段时，基础行的交叉就是逆势单，柱子说什么都不改变这一点；×4 已经转过来时，同一个交叉才是顺势延续。两行同时给出极值区交叉，是最稀有也最好的一种。\n\n柱子格与同一行的 MACD 格互相矛盾——数值还是正的、柱子已经在缩——那是转向的前半段，也正是该看这张表而不是盯一条线的理由。\n\n## 设置项\n\n**Position 位置**——表格放在子窗口的哪个角。**Compact 紧凑模式**收紧内边距与字号。\n\n**MACD / vs Signal / Histogram / Phase**——每列一个开关。\n\n**Recency Fade 时效淡化**——按状态年龄压暗，默认开启。\n\nMACD 的参数不在这里配置。每一行都按 MACD 引擎当前的 **Fast Length**、**Slow Length**、**Signal Length** 与两个均线类型计算，所以表格和它下面那条曲线，描述的永远是同一个指标。\n\n## 信号与提醒\n\n只有一类事件：**macdx_mtf_align**——三行首次在同一阶段上取得一致时触发，方向写在事件上，强度取基础行 MACD 的绝对值。交叉、柱子翻转与背离都属于产出它们的模块，请到那里挂提醒。\n\n分组按根数切、最新未满的一组丢弃，历史更深时切分位置可能不同。就单张已加载的图而言，表格完全确定，任何一格都不会自我改写。\n";
