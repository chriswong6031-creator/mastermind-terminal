// Guide content for the pulse/mtf premium-suite module — bundled (NOT served from public/):
// Next's standalone server does not serve public/ files added after the build, and guides
// ship with the build rather than being rsync'd like /data. Lazily imported by GuidePanel.
// Markdown is rendered through lib/md.ts (escape-safe).

export const en = "# MTF Dashboard\n\nThe pulse read at three depths at once — same wave, longer bars — in one corner table.\n\n## What you see\n\nThree rows: your chart's bars, then the same bars in groups of two, then in groups of four. Four cells across each row — **Wave** (which of the four phases the pulse is in), **Signal** (the last buy or sell marker and how many bars ago it printed), **Div** (the most recent divergence and its class) and **Extreme** (whether the wave is beyond ±60, and on which side). Cells fade as their state ages, so bright means fresh and dim means old news.\n\n**How the coarse rows are built — worth reading once.** Nothing is fetched. The ×2 and ×4 rows are the bars already on your chart, grouped: first open, highest high, lowest low, last close, volume summed. Three things follow. Groups are counted in bars, not in clock time, so on a daily chart the coarse rows are 2-day and 4-day candles rather than the week and the month. The newest group is excluded until it is complete, so the ×4 row can sit up to three bars behind the chart and only moves when a group closes. And each coarse row inherits the loaded history — the ×4 row has a quarter of the bars, so it needs a deep chart before its wave means anything.\n\nIn exchange, the table can never disagree with your chart because someone else built a higher-timeframe series differently, and no row repaints when a late higher-timeframe bar lands.\n\n## How to trade it\n\nAlignment, not prediction. Take chart-timeframe signals in the direction the two coarse rows already agree on, and let the base row do the timing. Both coarse rows rising with the base row in decay is a pullback inside a trend — the highest-quality version of the entry Pulse Signals prints.\n\nAll three rows agreeing at an extreme is the opposite reading: everything is stretched the same way and you are late to it.\n\nA base row that keeps flipping while both coarse rows sit unchanged is chop. That is the table earning its space — it says trade smaller, or not at all, without you changing timeframe to find out.\n\n## Settings\n\n**Position** — which corner of the pane holds the table. **Compact** tightens padding and type.\n\n**Wave / Signals / Divergences / Extremes** — one toggle per column.\n\n**Recency Fade** — the age dimming; off if only the current state matters.\n\nThe wave is not configured here. The table reads Pulse Wave's **Profile** and the Signals module's own settings as you have them, so all three rows and the pane below always describe the same wave.\n\n## Signals & alerts\n\nOne event: **pulse_mtf_align**, fired when all three rows agree on a direction they did not agree on before, direction on the event. Row-level things — a buy marker, a divergence — belong to the modules that produce them; alert there, not here.\n\nGroups are cut by bar count and the unfinished newest one is dropped, so the same symbol at a different history depth can slice them differently. Within one loaded chart the result is fully deterministic, and no cell rewrites itself.\n";
export const zh = "# 多周期面板（MTF Signals）\n\n同一条脉冲波，同时看三种粗细的K线，全部塞进角落里的一张表。\n\n## 图上看到什么\n\n三行：你图上的原始K线，然后是两根一组、四根一组的同一批K线。每行横向四格——**Wave**（脉冲处于四个阶段中的哪一个）、**Signal**（最近一次买卖标记，以及它是几根K线之前打出的）、**Div**（最近一次背离及其类别）与 **Extreme**（是否已越过 ±60，越的是哪一边）。格子会随状态变旧而淡化，所以亮的是刚发生的变化，暗的是旧闻。\n\n**粗行是怎么来的——请务必读一遍。** 这里不向服务器多取任何数据。×2 与 ×4 两行就是你图上已有的K线合并出来的：取第一根的开盘、区间内最高最高价、最低最低价、最后一根的收盘，成交量相加。由此有三个后果。分组按**根数**切，不按时钟切，所以在日线图上，粗行是\"两日线\"和\"四日线\"，而不是周线和月线。最新那一组在凑满之前会被剔除，因此 ×4 行可能比图面慢最多三根K线，并且只在一组走完时才会动。粗行还要继承已加载的历史——×4 行只有四分之一的K线数量，图上历史太浅时它的波形不具备意义。\n\n换来的东西值这个代价：这张表永远不会因为别人家的大周期序列拼法不同而与你的图打架，也永远不会因为一根迟到的大周期K线而改写已经显示过的状态。\n\n## 怎么用它交易\n\n看的是一致性，不是预测。只在两条粗行已经一致的方向上，接受原始周期的信号，并把择时交给基础行。两条粗行都在上行阶段、基础行处于衰减，就是趋势内部的一次回踩——脉冲信号模块打出的入场里，成色最好的正是这一类。\n\n三行在极值处完全一致，读法恰好相反：所有周期都朝同一边拉伸，你已经晚了。\n\n基础行来回翻、两条粗行纹丝不动，那就是震荡。这正是这张表存在的价值——不用你切换周期去确认，它就告诉你把仓位做小，或者干脆别做。\n\n## 设置项\n\n**Position 位置**——表格放在子窗口的哪个角。**Compact 紧凑模式**收紧内边距与字号。\n\n**Wave / Signals / Divergences / Extremes**——每列一个开关。\n\n**Recency Fade 时效淡化**——按状态年龄压暗。只关心当下状态就可以关掉它。\n\n波本身不在这里配置。表格会按你当前的设置去读脉冲波的 **Profile** 与信号模块的各项开关，所以三行与下方子窗口描述的永远是同一条波。\n\n## 信号与提醒\n\n只有一类事件：**pulse_mtf_align**——三行首次在同一方向上取得一致时触发，方向写在事件上。行内的东西（买卖标记、背离）属于产出它们的模块，请到那些模块上挂提醒。\n\n分组按根数切、最新未满的一组丢弃，因此同一标的在不同历史深度下，切分位置可能不同。就单张已加载的图而言，结果完全确定，任何一格都不会自我改写。\n";
