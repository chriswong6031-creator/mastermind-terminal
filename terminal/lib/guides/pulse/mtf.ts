// Guide content for the pulse/mtf premium-suite module — bundled (NOT served from public/):
// Next's standalone server does not serve public/ files added after the build, and guides
// ship with the build rather than being rsync'd like /data. Lazily imported by GuidePanel.
// Markdown is rendered through lib/md.ts (escape-safe).

export const en = "# MTF Dashboard\n\nThe pulse read at three depths at once — same wave, longer bar groupings — in one corner table.\n\n## What you see\n\nThree columns show **Chart**, **2×** and **4×**. Three rows show **State** (the current Pulse phase and wave value), **Signal** (the latest Pulse buy or sell and its age) and **Divergence** (the latest regular or hidden divergence and its age). Recent signal and divergence cells are brightest; older in-window events fade so stale context does not compete with a fresh print.\n\n**How the coarse columns are built — worth reading once.** Nothing is fetched from an independent timeframe feed. The 2× and 4× columns group the bars already loaded on your chart: first open, highest high, lowest low, last close, volume summed, then the Pulse calculation is rerun on each grouped series. Groups are counted in bars, not clock time, so on a daily chart they are 2-day and 4-day blocks rather than weekly and monthly candles. The newest coarse group is excluded until complete, so 4× can sit up to three chart bars behind and only moves when a full group closes. It also has one quarter as many observations, so it needs deeper loaded history before the wave settles.\n\nThat deliberate lag is the non-repaint rule made visible: a completed 2× or 4× cell is never revised by an in-progress aggregate.\n\n## How to trade it\n\nAlignment, not prediction. Let the 2× and 4× columns define context, then use Chart for timing. If both coarse columns show a constructive State while Chart briefly shows Decay, that is a pullback inside a broader push — the setting in which a fresh Chart buy signal is most useful.\n\nAll three columns stretched to the same extreme is the opposite reading: every depth is already extended and a new entry is late. If Chart keeps changing while 2× and 4× barely move, the table is identifying chop; trade smaller or stand aside.\n\n## Settings\n\n**Position** — which corner of the pane holds the table.\n\n**Compact** — tightens row height, padding and type for a short pane.\n\nThose are the module's only two controls. The dashboard has no row toggles, column toggles or recency-fade setting. It reads the suite-wide Pulse **Profile** and the live divergence preference automatically, so the dashboard and the other Pulse modules stay on the same calculation.\n\n## Signals & alerts\n\nThis dashboard emits no alert events. Its cells summarize state, signals and divergences computed for the three resampled columns; create alerts from the Pulse Signals or Divergences modules that own those event types.\n\nThe coarse columns use complete fixed bar groups only. Within one loaded chart the result is deterministic, and no completed dashboard cell repaints.\n";
export const zh = "# 多周期面板（MTF Dashboard）\n\n同一条脉冲波，同时看三种深度——相同逻辑、更长的K线分组——全部收进角落里的一张表。\n\n## 图上看到什么\n\n三列依次是 **Chart（图表）**、**2×** 与 **4×**。三行依次是 **State（状态）**，显示当前脉冲阶段与波值；**Signal（信号）**，显示最近一次脉冲买卖及距今多久；**Divergence（背离）**，显示最近一次常规或隐藏背离及距今多久。刚发生的信号与背离最亮，仍在有效窗口内的旧事件会逐渐变淡，让旧背景不会抢过新变化。\n\n**粗列是怎么来的——请务必读一遍。** 这里不读取独立周期行情源。2× 与 4× 列把图上已经加载的K线分组：取第一根开盘、区间最高价、区间最低价、最后一根收盘，成交量相加，然后在每条分组序列上重新计算脉冲。分组按**根数**切，不按时钟切，因此在日线图上它们是两日与四日分组，不是周线与月线。最新粗分组在凑满之前会被剔除，所以 4× 最多可能落后图表三根K线，并且只在完整分组收盘后更新。它的观察数量也只有原来的四分之一，因此需要更深的已加载历史才能稳定。\n\n这种刻意的滞后，就是不重绘规则的可见结果：已经完成的 2× 或 4× 单元格不会被仍在形成的分组改写。\n\n## 怎么用它交易\n\n看的是一致性，不是预测。让 2× 与 4× 两列负责背景，把 Chart 列用于择时。两条粗列都处于建设性状态，而 Chart 短暂进入 Decay（衰减），通常是大方向内部的一次回踩——此时新出现的 Chart 买入信号最有价值。\n\n三列同时向同一极值拉伸，读法恰好相反：三个深度都已经延展，新入场往往太晚。若 Chart 反复变化、2× 与 4× 几乎不动，面板指出的就是震荡；缩小仓位，或干脆不做。\n\n## 设置项\n\n**Position 位置**——表格停靠在子窗口的哪个角。\n\n**Compact 紧凑模式**——缩短行高、内边距与字号，适合较矮的子窗口。\n\n本模块只有这两个控件，没有行开关、列开关或时效淡化设置。面板会自动读取整个脉冲套件共用的 **Profile** 与当前背离偏好，因此它和其他脉冲模块始终使用同一套计算。\n\n## 信号与提醒\n\n本面板不会产生提醒事件。各单元格只是汇总三个重采样列上的状态、信号与背离；如需提醒，请到真正拥有这些事件的脉冲信号或背离模块中创建。\n\n粗列只使用完整的固定K线分组。就单张已加载的图而言，结果完全确定，已经完成的面板单元格不会重绘。\n";
