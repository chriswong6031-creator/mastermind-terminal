// Guide content for the rsix/mtf premium-suite module — bundled (NOT served from public/):
// Next's standalone server does not serve public/ files added after the build, and guides
// ship with the build rather than being rsync'd like /data. Lazily imported by GuidePanel.
// Markdown is rendered through lib/md.ts (escape-safe).

export const en = "# MTF Dashboard\n\nThe same RSI at three depths, in one corner table — so you can see whether the coarser groupings agree before you act on the pane.\n\n## What you see\n\nThree rows: your chart's bars, then the same bars in groups of two, then in groups of four. Across each row — **RSI** (the reading itself), **Zone** (overbought at 65 and above, oversold at 35 and below, neutral between), **Trend** (which side of 50 the wave sits on, or of its smoothing MA when that is on), **Signal** (the last reversal triangle and how many bars ago it printed) and **Div** (the most recent divergence). Cells fade as their state ages, so a bright cell is a fresh change.\n\n**How the coarse rows are built — worth reading once.** Nothing is fetched. The ×2 and ×4 rows are the bars already on your chart, grouped: first open, highest high, lowest low, last close, volume summed, with the RSI recomputed on that shorter series. Three things follow. Groups are counted in bars, not clock time, so on a daily chart the coarse rows are 2-day and 4-day candles rather than the week and the month. The newest group is excluded until it fills, so the ×4 row can sit up to three bars behind the chart and only moves when a group closes. And a coarse row inherits the loaded history — the ×4 row has a quarter of the bars, so a short chart makes it thin.\n\nIn exchange, the table can never disagree with your pane because a vendor built its higher timeframe differently, and no row repaints when a late higher-timeframe bar lands.\n\n## How to trade it\n\nCoarse rows filter, base row triggers. An oversold base row while the ×4 row sits above 50 is a pullback inside strength — the setting in which an RSI Signals triangle is worth taking. The same triangle with every row below 50 is a countertrend fade of the strongest thing on the chart.\n\nZone agreement across all three rows is a stretched market, not an entry: a ×4 row that is genuinely overbought usually needs a real correction, not a scalp against it.\n\nA divergence on the ×2 or ×4 row carries more weight than one on the base row — it took four times as much price action to build.\n\n## Settings\n\n**Position** — which corner of the pane holds the table. **Compact** tightens padding and type.\n\n**RSI / Zone / Trend / Signals / Divergences** — one toggle per column.\n\n**Recency Fade** — the age dimming, on by default.\n\nThe RSI is not configured here. Every row uses RSI Engine's **RSI Length**, **Source** and **Smoothing MA** exactly as you have them, so the table and the wave below it can never describe two different indicators.\n\n## Signals & alerts\n\nOne event: **rsix_mtf_align**, fired when all three rows agree on a side of 50 they did not agree on before, direction on the event and the base row's distance from 50 as strength. Reversal triangles, deviation touches and divergences belong to the modules that produce them — alert there.\n\nGroups are cut by bar count and the unfinished newest one is dropped, so a deeper history can slice them differently. Within one loaded chart the table is fully deterministic and no cell rewrites itself.\n";
export const zh = "# 多周期面板（MTF Dashboard）\n\n同一条 RSI，三种粗细的K线，收进角落里的一张表——动手之前先看看更粗的分组同不同意。\n\n## 图上看到什么\n\n三行：你图上的原始K线，然后是两根一组、四根一组的同一批K线。每行横向依次是 **RSI**（读数本身）、**Zone**（65 及以上为超买，35 及以下为超卖，中间为中性）、**Trend**（波线站在 50 的哪一侧；打开平滑均线时则看它站在均线的哪一侧）、**Signal**（最近一个反转三角，以及它是几根之前打出的）与 **Div**（最近一次背离）。格子随状态变旧而淡化，亮的就是刚发生的变化。\n\n**粗行是怎么来的——请务必读一遍。** 这里不向服务器多取任何数据。×2 与 ×4 两行就是你图上已有的K线合并出来的：第一根的开盘、区间最高价、区间最低价、最后一根的收盘、成交量相加，然后在这条更短的序列上重算 RSI。由此有三个后果。分组按**根数**切，不按时钟切，所以在日线图上，粗行是\"两日线\"和\"四日线\"，而不是周线和月线。最新一组在凑满之前会被剔除，因此 ×4 行可能比图面慢最多三根K线，并且只在一组走完时才动。粗行继承已加载的历史——×4 行只有四分之一的K线，图上历史太浅它就会很单薄。\n\n换来的是：这张表不会因为哪家数据商的大周期拼法不同而与你的子窗口打架，也不会因为一根迟到的大周期K线而改写已显示的状态。\n\n## 怎么用它交易\n\n粗行做过滤，基础行做触发。基础行超卖、×4 行仍在 50 之上，这是强势内部的一次回踩——RSI 信号模块的三角形值得接的正是这种环境。同样一个三角，若三行全在 50 之下，那就是在对抗图上最强的那股力量。\n\n三行的区域状态完全一致，说明市场被拉伸了，不是入场点：×4 行真的超买时，通常需要一次像样的调整，而不是一次逆势的短打。\n\n出现在 ×2 或 ×4 行上的背离，比基础行上的更有分量——它是用四倍的价格行为堆出来的。\n\n## 设置项\n\n**Position 位置**——表格放在子窗口的哪个角。**Compact 紧凑模式**收紧内边距与字号。\n\n**RSI / Zone / Trend / Signals / Divergences**——每列一个开关。\n\n**Recency Fade 时效淡化**——按状态年龄压暗，默认开启。\n\nRSI 不在这里配置。每一行都按 RSI 引擎当前的 **RSI Length**、**Source** 与 **Smoothing MA** 计算，所以表格和它下面那条波，永远不会是两个不同的指标。\n\n## 信号与提醒\n\n只有一类事件：**rsix_mtf_align**——三行首次在 50 的同一侧取得一致时触发，方向写在事件上，强度取基础行距离 50 的幅度。反转三角、跟随位触及与背离都属于产出它们的模块，请到那里挂提醒。\n\n分组按根数切、最新未满的一组丢弃，历史更深时切分位置可能不同。就单张已加载的图而言，表格完全确定，任何一格都不会自我改写。\n";
