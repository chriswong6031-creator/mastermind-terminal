// Guide content for the rsix/mtf premium-suite module — bundled (NOT served from public/):
// Next's standalone server does not serve public/ files added after the build, and guides
// ship with the build rather than being rsync'd like /data. Lazily imported by GuidePanel.
// Markdown is rendered through lib/md.ts (escape-safe).

export const en = "# MTF Dashboard\n\nThe same RSI at three depths, in one corner table — so you can see whether the coarser groupings agree before acting on the chart column.\n\n## What you see\n\nThree columns show **Chart**, **2×** and **4×**. Three rows show **RSI** (the current reading, slope and zone colour), **Signal** (the latest in-zone reversal and its age) and **Divergence** (the latest regular or hidden divergence and its age). Recent signal and divergence cells are brightest; older in-window events fade so current context stays legible.\n\n**How the coarse columns are built — worth reading once.** Nothing is fetched from an independent timeframe feed. The 2× and 4× columns group the bars already loaded on your chart — first open, highest high, lowest low, last close and summed volume — then recompute RSI on each shorter series. Groups are counted in bars, not clock time, so on a daily chart they are 2-day and 4-day blocks rather than weekly and monthly candles. The newest coarse group is excluded until complete, so 4× can sit up to three chart bars behind and only changes when a full group closes. It also has one quarter as many observations, so shallow history gives it less warm-up.\n\nThat deliberate lag is the non-repaint rule made visible: a completed 2× or 4× cell is never revised by an in-progress aggregate.\n\n## How to trade it\n\nCoarse columns filter; Chart triggers. An oversold Chart reading while 4× remains constructive is a pullback inside broader strength — the environment in which a fresh RSI reversal signal is worth considering. The same Chart signal while all three columns are weak is a countertrend fade.\n\nIf every column is stretched into the same zone, treat it as extension rather than an entry. A divergence in 2× or 4× represents a pattern built from more underlying price action than the Chart equivalent, but it is still context until price confirms.\n\n## Settings\n\n**Position** — which corner of the pane holds the table.\n\n**Compact** — tightens row height, padding and type for a short pane.\n\nThose are the module's only two controls. The dashboard has no row toggles, column toggles or recency-fade setting. It reads the live RSI Engine calibration and the current divergence preference automatically, so all three columns use the same suite settings as the rest of RSI Ultimate.\n\n## Signals & alerts\n\nThis dashboard emits no alert events. Its cells summarize RSI state, reversal signals and divergences computed for the three resampled columns; create alerts from the RSI Signals or Divergences modules that own those event types.\n\nThe coarse columns use complete fixed bar groups only. Within one loaded chart the result is deterministic, and no completed dashboard cell repaints.\n";
export const zh = "# 多周期面板（MTF Dashboard）\n\n同一条 RSI，同时看三种深度，收进角落里的一张表——在按图表列行动之前，先看更粗的分组是否同意。\n\n## 图上看到什么\n\n三列依次是 **Chart（图表）**、**2×** 与 **4×**。三行依次是 **RSI**，显示当前读数、斜率与区域配色；**Signal（信号）**，显示最近一次区间内反转及距今多久；**Divergence（背离）**，显示最近一次常规或隐藏背离及距今多久。刚发生的信号与背离最亮，仍在有效窗口内的旧事件会逐渐变淡，让当前背景保持清晰。\n\n**粗列是怎么来的——请务必读一遍。** 这里不读取独立周期行情源。2× 与 4× 列把图上已经加载的K线分组——第一根开盘、区间最高价、区间最低价、最后一根收盘与成交量合计——再在每条更短的序列上重算 RSI。分组按**根数**切，不按时钟切，因此在日线图上它们是两日与四日分组，不是周线与月线。最新粗分组在凑满之前会被剔除，所以 4× 最多可能落后图表三根K线，并且只在完整分组收盘后更新。它的观察数量也只有原来的四分之一，因此较浅的历史会减少预热数据。\n\n这种刻意的滞后，就是不重绘规则的可见结果：已经完成的 2× 或 4× 单元格不会被仍在形成的分组改写。\n\n## 怎么用它交易\n\n粗列做过滤，Chart 列做触发。Chart 超卖而 4× 仍保持建设性状态，是大方向强势中的一次回踩——新 RSI 反转信号值得考虑的正是这种环境。同一个 Chart 信号若三列全部偏弱，就是逆势交易。\n\n三列同时拉伸到同一极值区，应当读作延展，而不是入场。2× 或 4× 的背离由更多底层价格行为构成，通常比 Chart 上的同类形态更有分量，但在价格确认之前仍只是背景。\n\n## 设置项\n\n**Position 位置**——表格停靠在子窗口的哪个角。\n\n**Compact 紧凑模式**——缩短行高、内边距与字号，适合较矮的子窗口。\n\n本模块只有这两个控件，没有行开关、列开关或时效淡化设置。面板会自动读取 RSI 引擎的实时参数与当前背离偏好，因此三列和 RSI Ultimate 其他模块始终使用同一套设置。\n\n## 信号与提醒\n\n本面板不会产生提醒事件。各单元格只是汇总三个重采样列上的 RSI 状态、反转信号与背离；如需提醒，请到真正拥有这些事件的 RSI 信号或背离模块中创建。\n\n粗列只使用完整的固定K线分组。就单张已加载的图而言，结果完全确定，已经完成的面板单元格不会重绘。\n";
