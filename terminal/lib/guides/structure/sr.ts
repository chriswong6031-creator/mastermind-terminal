// Guide content for the structure/sr premium-suite module — bundled (NOT served from public/):
// Next's standalone server does not serve public/ files added after the build, and guides
// ship with the build rather than being rsync'd like /data. Lazily imported by GuidePanel.
// Markdown is rendered through lib/md.ts (escape-safe).

export const en = "# Smart S/R\n\nAutomatic support and resistance, ranked by how price has actually reacted there — not just how often it visited.\n\n## What you see\n\nHorizontal lines drawn from the level's first pivot to the right edge. Levels below the current price render in the support color, levels above in the resistance color. Line weight and brightness carry the ranking: the heavier and brighter the line, the more touches it has collected and the harder price has bounced off it.\n\nEach line carries a small chip at the right edge: the level's price and a **×N** touch count. When a level is broken by a closing bar it does not vanish silently — it turns dashed and faint for a while, so you can see what just gave way, then retires.\n\nWith **Buffer Zone** on, each line gains a slim shaded band around it — a reminder that a level is an area, not a laser line.\n\n## How to trade it\n\n1. Mark the two or three heaviest lines above and below the current price. Those are the levels the module has actual evidence for.\n2. Prefer trades that agree with a reaction: a bounce off a heavy support in an uptrend beats a guess in the middle of nowhere.\n3. Put stops beyond the level (or beyond its buffer band), never exactly on it.\n4. A close through a heavy level is information — the side that defended it just lost. The dashed after-image marks the spot; broken resistance often gets retested as support.\n5. When NOT to use this: in a fresh, fast trend the chart may simply have no tested levels nearby — an absence of lines is honest, not broken.\n\nThe ranking is derived from candle history — touches, forward reactions, and recency. It is evidence-weighting, not a prediction.\n\n## Settings\n\n**Sensitivity (High / Medium / Low, default Medium)** — High finds more, lighter levels; Low keeps only the strongest structures.\n\n**Min Touches (2–5, default 2)** — how many pivots a cluster needs before it becomes a level.\n\n**Show Last (2–12, default 6)** — how many top-ranked levels stay on the chart.\n\n**Buffer Zone (off)** — draws the shaded area band around each line.\n\n**Labels (on)** — the price + ×N chips at the right edge.\n\n## Signals & alerts\n\nTwo events feed the alert engine: a **break** (a bar closes through a level — direction tells you which side lost) and a **hold** (price tagged a level and it held). Confirmed levels never move — the anchor price is frozen at the first pivot, so history cannot be repainted.\n";

export const zh = "# 智能支撑阻力\n\n自动识别支撑与阻力位，并按价格在该位置的真实反应强度排序——而不只是按到访次数。\n\n## 图上都有什么\n\n从关键位首次形成处向右延伸的水平线。低于现价的线用支撑色，高于现价的线用阻力色。线的粗细与亮度代表评分：线越粗越亮，说明触碰次数越多、每次反弹越有力。\n\n每条线右端有一个小标签：价位与 **×N** 触碰次数。当某个价位被收盘价突破时，它不会悄悄消失——先变为虚线并淡出一段时间，让你看清刚刚被攻破的位置，然后才退场。\n\n开启 **Buffer Zone** 后，每条线周围会出现一条浅色带——提醒你关键位是一个区域，不是一条激光线。\n\n## 如何交易\n\n1. 标记现价上下最粗的两三条线，它们是模块有真实证据支持的位置。\n2. 优先做与反应一致的交易：上升趋势中在重型支撑处的反弹，胜过在半空中的猜测。\n3. 止损放在关键位（或其缓冲带）之外，不要正好压在线上。\n4. 收盘价穿越重型关键位本身就是信息——防守方刚刚失守。虚线残影标出位置；被突破的阻力常会回踩变成支撑。\n5. 何时不适用：在全新的快速趋势中，附近可能确实没有经过检验的关键位——没有线是诚实的表现，不是故障。\n\n排序来自K线历史——触碰、后续反应与时效衰减。它是证据加权，不是预测。\n\n## 设置\n\n**Sensitivity 灵敏度（High / Medium / Low，默认 Medium）** — High 找到更多但更轻的位置；Low 只保留最强结构。\n\n**Min Touches 最少触碰（2–5，默认 2）** — 一个聚类需要多少个枢轴点才能成为关键位。\n\n**Show Last 显示数量（2–12，默认 6）** — 图上保留多少条评分最高的线。\n\n**Buffer Zone 缓冲带（默认关）** — 在每条线周围绘制区域带。\n\n**Labels 标签（默认开）** — 右端的价位 + ×N 标签。\n\n## 信号与提醒\n\n两类事件接入提醒引擎：**突破**（收盘价穿越关键位——方向告诉你哪一方失守）与**守住**（价格触碰后未破）。已确认的关键位永不移动——锚定价冻结在首个枢轴点上，历史不会被重绘。\n";
